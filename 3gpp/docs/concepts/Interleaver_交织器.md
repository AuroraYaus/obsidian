---
type: definition
aliases:
  - Interleaver
  - 交织器
  - 交织算法
  - 三角交织器
  - triangular interleaver
  - 子块交织
tags:
  - 3gpp
  - concepts
  - interleaver
source_spec: "TS 36.212 §5.1.3.2.3/§5.1.4.1.1; TS 38.212 §5.4.1.3/Table 5.4.1.1-1/§5.4.2.2; TS 38.211 §7.3.1.5"
queries: 1
---

# Interleaver 交织器

交织器（interleaver）是把比特/符号序列按确定性规则重排顺序的部件：输入顺序 $c_0,c_1,\ldots$，输出按置换 $\pi$ 重排为 $c_{\pi(0)},c_{\pi(1)},\ldots$。它不改变比特数量、不改变码率，只改变顺序——目的是把突发错误（深衰落、打孔、干扰造成的连续错位）摊平成孤立错误，让纠错码能分别修复。3GPP 协议中没有"万能交织器"，而是按用途分化出一族结构：LTE Turbo 用代数置换 QPP，NR UCI 用三角交织器，速率匹配用矩形子块交织，下行资源映射用伪随机 VRB 置换，概率整形（PS）扩展再用 SBPM 做位组织。

## 独立解释任务

任务目标：把 3GPP 协议中的交织器家族梳理成一张全景图——三角交织、QPP、子块交织、块交织、SBPM、VRB 映射各自解决什么问题、协议锚点在哪、按什么维度区分，让读者遇到任意一种交织器都能定位到它的结构、用途与讲义落点。

## 科学定义

### 通用定义

交织是一个**确定性置换**：$\pi: \{0,\ldots,N-1\} \to \{0,\ldots,N-1\}$ 是一一映射，输出序列 $c'$ 由 $c'_j = c_{\pi(j)}$ 生成。关键性质：(1) 一一对应（无重复无遗漏）；(2) 不改变比特数与码率；(3) 收发双方必须使用同一置换（或逆置换）——任一侧算错参数，交织/解交织整体错位。

### 家族全景（按用途与结构）

| 交织器 | 结构 | 协议位置 | 用途 |
|:---|:---|:---|:---|
| QPP 交织器 | 代数置换 $\Pi(j)=(f_1j+f_2j^2)\bmod K$ | TS 36.212 §5.1.3.2.3 + Table 5.1.3-3 | LTE Turbo 内部交织器（两个 RSC 之间），无冲突支持并行译码 |
| 三角交织器 | 三角形阵列（行递减），按行写、按列读 | TS 38.212 §5.4.1.3（$I_{BIL}=1$ 门控） | NR UCI 在 PUSCH/PUCCH 上的散布，变长输入友好 |
| 子块交织器 | 矩形 $R\times32$ 块交织，按行写、按列读 | TS 36.212 §5.1.4.1.1（LTE）；TS 38.212 Table 5.4.1.1-1（Polar）；§5.4.2.2（LDPC bit interleaving） | 速率匹配前的比特散布 |
| 块交织器 | 矩形 $R\times C$，按行写、按列读 | 上述子块交织的通用原型；LTE 比特交织 | 固定列数场景，间距 = 列数 |
| SBPM | 矩形转置交织（4^k 块组织） | 非 3GPP 标准（PS 扩展，叠加在 §5.4.2.2 之后） | 概率整形：把 shaped bits 放到 QAM label 幅度位 |
| VRB 交织映射 | 伪随机置换，粒度 2/4 PRB 一束 | TS 38.211 §7.3.1.5；TS 38.214 §5.1.2.3 | 下行 VRB→PRB 频率分集 |

### 成员速览

- **QPP**：$\Pi(j)=(f_1j+f_2j^2)\bmod K$，$f_1/f_2$ 由协议对 188 个合法 $K$（40~6144）逐行固定；二次项保证伪随机打散，同时满足并行译码无冲突（contention-free）。
- **三角交织器**：行数 $L$ 取满足 $L(L+1)/2 \ge G$ 的最小整数；按行写入、按列读出，间距随位置渐变（G=10 例：4/3/2）。对任意长度 $G$ 最多浪费 $L-1$ 空位——UCI 长度每次不同，故选三角形。
- **子块交织**：LTE 三路编码流各自过同一个 32 列块交织，输出 $K_\Pi$（恒为 32 倍数，补 `<NULL>`）；NR Polar 用 32 项 pattern（Table 5.4.1.1-1）；NR LDPC 的 bit interleaving 按调制阶数 $Q_m$ 排列。
- **SBPM**：与标准交织对象不同——它解决"shaped bits 必须落在幅度位"的位置语义问题，叠加在标准交织之后。

## 直观模型

交织像**洗牌**：牌的内容和数量不变，只是顺序变了。不同洗法对应不同交织器——QPP 像按代数公式精确指定每张牌的新位置（可复现、可并行）；三角交织像把牌摆成递减的三角形再按列收拢（适应任意张数）；块交织像把牌排成矩形、按行铺按列收（间距固定=列数）。"交织不减少错误数，只改变错误分布"——就像洗牌不改变牌面，只改变霉运落在哪几张牌上：连续 3 张烂牌（突发错误）被洗散后，纠错码能分别处理每一张。

## 常见误解

| 误解 | 正确理解 |
|:---|:---|
| 交织会改变码率 | 交织只改变比特顺序，不改变比特数量与码率。 |
| 三角交织器的"三角"与 MIMO 球面检测有关 | 三角是写入阵列形状（行递减）；MIMO sphere 的球是检测搜索空间——两回事。 |
| 三角交织和矩形块交织可以互换 | 三角间距渐变、适应任意长度 G；块交织间距固定=列数、要求长度是列数整数倍或大量补位——UCI 变长所以用三角。 |
| QPP 参数可以自行调优 | $f_1/f_2$ 由 TS 36.212 Table 5.1.3-3 对 188 个 K 逐行固定，取错行地址即错。 |
| SBPM 就是标准交织 | 标准交织是 TS 38.212 §5.4.2.2 的 LDPC bit interleaving；SBPM 是非标准 PS 扩展，叠加其后，对象与目的完全不同。 |
| 交织器越多越好 | 每个交织器解决特定问题（Turbo 内部/速率匹配/UCI 散布/资源映射），按协议用途选择，多交织只增加时延与硬件。 |

## 协议锚点

- **QPP（LTE Turbo 内部）**：TS 36.212 Rel-19 `36212-j30` §5.1.3.2.3 + Table 5.1.3-3（188 组 $K/f_1/f_2$），本地 `tables/table_0009.csv/html`。
- **LTE 子块交织**：TS 36.212 §5.1.4.1.1（32 列，$K_\Pi$、`<NULL>` 填充），本地 `content.md:791-819`、`table_0010.csv/html`。
- **三角交织器（NR UCI）**：TS 38.212 §5.4.1.3（$I_{BIL}=1$ 门控，Polar rate matching 内部），本地抽取件 `full.md §5.4.1.3` 已核对。
- **NR Polar 子块交织**：TS 38.212 Table 5.4.1.1-1（32 项 pattern，对应本地 `table_0019.csv/html`）。
- **NR LDPC bit interleaving**：TS 38.212 §5.4.2.2（$Q_m$ 相关排列）。
- **VRB 交织映射**：TS 38.211 §7.3.1.5；TS 38.214 §5.1.2.3（`vrb-to-prb-mapping` 字段）。
- **SBPM**：非 3GPP 标准（PS 扩展），无标准小节；仿真器实现见 `+ProbShaping/`。

## 图谱关联

- [[概念图谱入口]]
- [[QPP_Interleaver_QPP交织器]]（LTE Turbo 内部交织）
- [[SBPM_整形比特位置映射]]（PS 位组织层）
- [[PUCCH_上行控制信道与UCI]]（UCI 承载语境 → 三角交织器）
- [[T10.9_NR_UCI_interleaving_triangular|T10.9]]（三角交织主阵地，含 G=10 手算与协议锚点核对）
- [[T7.2_LTE_subblock_deinterleaver_circular_buffer|T7.2]]（LTE 子块交织/循环缓存）
- [[T10.7_NR_Polar_rate_recovery|T10.7]]（NR Polar 子块交织 pattern）
- [[T9.4_NR_LDPC_bit_deinterleaving|T9.4]]（NR LDPC bit deinterleaving 与 LLR 放置）
- [[TX2_scrambling_interleaving|TX2]]（块交织教学例）
- 关系语义：交织器家族是"顺序重排"这一单一职责按用途分化的结果——QPP 服务 Turbo 迭代译码，子块交织服务速率匹配，三角交织服务 UCI 变长散布，SBPM 服务概率整形的位置语义，VRB 映射服务资源分集；它们共同保证"突发错误摊平成孤立错误"这一工程目标。
