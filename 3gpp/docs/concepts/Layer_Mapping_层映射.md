---
type: definition
aliases:
  - Layer Mapping
  - 层映射
  - 空间层
  - Codeword to Layer
tags:
  - 3gpp
  - concepts
  - tx-chain
  - mimo
source_spec: "TS 38.211 Rel-19 §7.3.1.3"
---

# Layer Mapping 层映射

层映射是把一个码字（codeword）的 QAM 符号分发到一个或多个**空间层**（layer）的步骤。每层是一个独立数据流，可并行传输——层数就是空间复用的维度。

## 独立解释任务

任务目标：解释层（layer）为什么不是天线，以及层映射的"分发规则"为什么必须收发一致。

## 科学定义

层映射规则（TS 38.211 §7.3.1.3）：

- 单层：$x^{(0)}(i) = d(i)$——所有符号进第 0 层
- 两层：$x^{(0)}(i) = d(2i)$、$x^{(1)}(i) = d(2i+1)$——交替分发（奇偶）
- 三层/四层：按固定模式分发（三层 3-2-3-2 循环，四层 2-2-2-2）

约束：层数 ≤ 信道秩 ≤ min(Ntx, Nrx)；单码字最多 4 层（仿真器配置）。

## 直观模型

层映射像"发牌"：一副牌（码字符号）按固定规则分给几个玩家（层）。发牌顺序（规则）必须事先约定——接收端要知道"第 i 张牌在谁手里"才能收回。层是数据流的概念，不是硬件天线。

## 常见误解

| 误解 | 正确理解 |
|:---|:---|
| 层 = 天线 | 层是空间数据流，天线是物理硬件；4 层传输可能用 8 根天线（预编码分配） |
| 层数越多一定越好 | 层数受信道秩限制，低秩/低 SNR 时多层反而干扰大 |
| 层映射规则可以随意改 | 规则由标准固定，收发必须一致——任何一方算错，数据全废 |
| 单码字只能 1 层 | 单码字可以映射到最多 4 层（并行流共享一个码字） |

## 协议锚点

- 层映射规则：TS 38.211 Rel-19 §7.3.1.3。
- 层数与码字：TS 38.214 Rel-19 §5.1。
- 本地锚点：`3GPP_Rel19/processed/TS_38.211_38211-j30/content.md`。
- 仿真器实现：`nrLayerMap`（MATLAB 5G Toolbox）、`+phy/+mimo/build_mimo_info.m`。

## 图谱关联

- [[概念图谱入口]]
- [[MIMO_多天线系统]]
- [[Precoding_预编码]]
- [[TX3_layer_mapping_precoding|TX3 层映射与预编码讲义]]
- [[DMRS_解调参考信号]]
- 关系语义：层映射（数据分发）→ 预编码（端口加权）→ RE 映射，是 MIMO 发射链的三连步；每层配一个 DMRS 端口用于接收端解调。
