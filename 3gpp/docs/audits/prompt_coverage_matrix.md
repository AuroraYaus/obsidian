---
type: spec
aliases:
  - prompt coverage matrix
tags:
  - 3gpp
  - docs
  - audit
source_spec: "docs/audits/prompt_coverage_matrix.md"
---
> ⚠️ 本档案为 2026-06-21 快照（基于旧编号与 PNG 时代状态），内容已过期，仅作历史参考；当前状态以审计工具实时输出与 `image_asset_inventory.md` 为准。

# Prompt Coverage Matrix

审计时间：2026-06-21  
审计范围：`2026-06-19-lte-nr-decoding-learning-roadmap.md` 与已存在的 `docs/L1_基础/**/*.md`、`docs/L2_协议算法/**/*.md`、`docs/L3_工程实现/**/*.md`。当前 L3 已纳入模块 12 的 T17.1-T17.5、模块 13 的 T18.1-T18.6、模块 14 的 T19.1-T19.6 和模块 15 的 T20.1-T20.6。  
约束：本报告读取讲义正文、图片脚本和审计记录；矩阵用于覆盖追踪，不替代单篇审核。

## 摘要

已覆盖已存在讲义 94 篇：L1 28 篇，L2 43 篇，L3 模块 12 共 5 篇，L3 模块 13 已完成 6 篇，L3 模块 14 已完成 6 篇，L3 模块 15 已完成 6 篇。Prompt 是最低覆盖线，不是写作上限；本矩阵只用于定位 Prompt 点名要求、正文证据和后续补写建议，不能替代正文的理论解释、协议精读、对象模型、数值例子、工程验证和图表直观化。整体上，已存在讲义基本都包含学习目标、前置知识、正文证据位置、接收端/工程位置、验收或自测、证据记录与参考文献。当前文件名差集检查确认 `docs/L1_基础/T*.md`、`docs/L2_协议算法/T*.md`、`docs/L3_工程实现/T12*.md`、`docs/L3_工程实现/T18.1*.md`、`docs/L3_工程实现/T18.2*.md`、`docs/L3_工程实现/T18.3*.md`、`docs/L3_工程实现/T18.4*.md`、`docs/L3_工程实现/T18.5*.md`、`docs/L3_工程实现/T18.6*.md`、`docs/L3_工程实现/T19.1*.md`、`docs/L3_工程实现/T19.2*.md`、`docs/L3_工程实现/T19.3*.md`、`docs/L3_工程实现/T19.4*.md`、`docs/L3_工程实现/T19.5*.md`、`docs/L3_工程实现/T19.6*.md`、`docs/L3_工程实现/T20.1*.md`、`docs/L3_工程实现/T20.2*.md`、`docs/L3_工程实现/T20.3*.md`、`docs/L3_工程实现/T20.4*.md`、`docs/L3_工程实现/T20.5*.md` 和 `docs/L3_工程实现/T20.6*.md` 均已纳入本矩阵；新增专题 `T0.1`、`T8.0`、`T9.0` 已按 depth backlog A1-A3 补入；主要缺口集中在三类：

1. 部分基础课对 roadmap 要求的“固定输入 Python 校验”“手算例子”覆盖充分，但协议锚点仍是背景级或 `待核验`，建议保持边界说明，不应误补成协议结论。
2. 部分协议课已经重建本地证据表，但真实 3GPP 表/图/公式的完整复现状态不一致，建议在讲义证据表中逐条关闭 `待核验` 条件。
3. T7.3 对软缓存、RV、增量冗余、重传 LLR 更新、循环缓存起点/区域、小型合并例子和定点饱和均有正文证据；TS 36.212 中 `N_cb`、`N_IR`、`E`、`k_0`、`<NULL>` 跳过和循环缓存选择语义已在正文重建。仍保留的边界是 TS 36.213/36.321 中 HARQ 过程控制、RV 调度来源和 MAC/PHY 反馈时序的精确锚点。

## L1 覆盖矩阵

| 讲义 | Roadmap Prompt 要求 | Roadmap 验收要求 | 正文中已有证据位置 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|:---|
| `docs/L0_协议阅读引导/T0.1_LTE_NR_decoder_protocol_reading_map.md` | 新增协议阅读地图，覆盖 LTE/NR 双侧协议、译码边界、问题提纲、三条主线和证据链。 | 能从 CRC、TB/CB、filler、Turbo、LDPC、Polar、rate matching、HARQ、MCS/TBS、soft buffer、descriptor、验证证据定位到协议和后续章节。 | `## 本节定位`、`## 协议族和译码边界`、`## 问题提纲`、`## 三条主线学习路径`、`## Python 协议地图图`、`## 执行与证据记录`。 | 无阻塞缺口；这是全项目入口图和索引讲义，不替代后续协议表、公式和 bit-exact 章节。 | 后续新增协议章节时回链本节，并保持 `docs/L0_协议阅读引导/assets/T0.1_LTE_NR_decoder_protocol_reading_map.png` 与正文同步。 |
| `docs/L1_基础/T1.1_GF2_binary_arithmetic_for_decoders.md` | GF(2) 支撑二元运算；异或加法、与乘法、多项式表示、CRC/奇偶校验；白话直觉、真值表、两个手算例子、固定输入 Python。 | 能手算 GF(2) 加法、乘法、多项式加法和 CRC 多项式除法一步。 | `## GF(2) 的定义与来源`、`## 加法：异或就是 GF(2) 加法`、`## 乘法：与运算就是 GF(2) 乘法`、`## 手算例题`、`## Python 校验片段`、`## 本节验收`。 | 协议引用停留在 CRC 动机层，未逐条列 TS 36.212/38.212 CRC 使用位置和本地锚点细节。 | 在 `## 和 3GPP 协议的关系` 增加一个“小节不复现协议多项式，只定位后续 CRC 入口”的表，列出 TS、章节、本地路径和关闭条件。 |
| `docs/L1_基础/T1.2_GF2_polynomials_crc_remainders.md` | GF(2) 多项式长除法是 CRC 算术核心；整数除法类比；8 bit 消息加 CRC-4；LTE/NR CRC 多项式只引用不复现。 | 能手算短 CRC 余数并解释零 syndrome。 | `## 手算 CRC 长除法`、`## 接收端零余数检查`、`## 例题：完整发送端和接收端过程`、`## Python 校验片段`、`## 和 3GPP 协议的关系`。 | 已有协议关系，但“只引用、暂不复现 LTE/NR CRC 生成多项式”的边界可更醒目。 | 在协议关系表增加“本节不复现 LTE/NR 生成多项式；T3.1 负责协议多项式复现”的明确行。 |
| `docs/L1_基础/T1.3_GF2_vectors_matrices.md` | GF(2) 向量、矩阵、转置、乘法、秩、稀疏矩阵；连接 LDPC 校验矩阵；3x6 例子、手算 syndrome、Python。 | 能用二元向量乘校验矩阵并解释非零 syndrome。 | `## 矩阵乘向量：每一行做一次奇偶检查`、`## 非零校验子的含义`、`## 手算例子：单比特错误与校验子列`、`## Python 校验片段`、`## 概念深化：教学矩阵到稀疏校验图`。 | LDPC 连接是概念桥接，未明确 Table 5.3.2-1/2/3 待后续重建。 | 在 `## 和 3GPP 协议的关系` 补一行“TS 38.212 基图表值不在本节复现，见 T3.4/T8.3”。 |
| `docs/L1_基础/T1.4_probability_bayes_soft_decoding.md` | 零概率论基础讲概率、条件概率、先验、似然、后验、证据、贝叶斯；噪声比特场景；两个数值例子。 | 能计算简单后验概率并解释 demapping 中 likelihood。 | `## 概率在译码中的必要性`、`## 条件概率`、`## 先验概率、似然、后验概率与证据项`、`## 贝叶斯公式`、`## 数值例子 1/2`。 | 协议锚点仅背景化，未把 TS 36.211/38.211 调制来源的待核验状态列为表。 | 在协议关系处增加“软信息来源待 T2.2/T2.3 精确锚定”的证据边界表。 |
| `docs/L1_基础/T1.5_LLR_soft_decision.md` | 从概率比推导硬判决、软判决和 LLR；解释符号、绝对值、为何不用原始概率；数值稳定、独立证据相加、饱和值。 | 能把概率转 LLR 并解释正、负、零、大、饱和 LLR。 | `## 概率比值形式的 LLR 定义`、`## 对数域表示的工程动机`、`## LLR 的正负号和幅度`、`## 裁剪、缩放和饱和`、`## 本节验收`。 | 独立证据相加有讲，但与 HARQ 软合并的接口可再显式指向 T4.3/T7.3。 | 在饱和或工程动机后加一段“后续 HARQ soft buffer 使用 LLR 相加，但需按定点饱和处理”。 |
| `docs/L1_基础/T1.6_information_theory_minimum_for_decoding.md` | 熵、互信息、容量、码率、编码增益；Turbo/LDPC/Polar 重要性；BSC 和 AWGN 直觉。 | 能解释码率、容量直觉和迭代软译码可靠性提升。 | `## 熵`、`## 互信息`、`## 码率`、`## 信道容量`、`## 手算例子：二元对称信道容量`、`## 噪声与信噪比的基础关系`。 | AWGN 例子偏直觉，未形成与 T2.1 噪声方差计算的可执行桥接。 | 增加一个 3 行小表：容量/码率/BLER 曲线分别由 T2.1、T4.5、L3 仿真承接。 |
| `docs/L1_基础/T2.9_AWGN_noise_scaling.md` | AWGN、高斯、SNR、Eb/N0、Es/N0、码率、调制阶数和噪声方差；推导 BPSK LLR；固定随机种子实验。 | 能计算给定码率、调制阶数、Eb/N0 的噪声方差并生成可复现 BPSK 样本。 | `## $E_b/N_0$、$E_s/N_0$、码率和调制阶数`、`## $E_b/N_0$ 到噪声方差的换算`、`## BPSK 在 AWGN 下的 LLR 推导`、`## Python 固定随机种子实验`。 | 3GPP 调制阶数连接只做背景；LTE 等价锚点仍待核验。 | 在协议关系表中把 TS 38.214 已定位项和 LTE 待核验项分两行列清，避免读者误认为 LTE 已关闭。 |
| `docs/L1_基础/T2.13_BPSK_QPSK_soft_demapping.md` | BPSK/QPSK 星座、Gray 映射、采样模型、精确/逐比特 LLR；Mermaid 图和小型数值例子。 | 能推导 BPSK LLR 并计算一个 QPSK 符号的 bit LLR。 | `## BPSK 一维星座模型`、`## QPSK 二维星座模型`、`## Gray 映射的可靠性收益`、`## QPSK 逐比特 LLR`、`## 手算例子`、`## 软解调流程图`。 | 协议调制条款仍标待核验，正文不应被当成 3GPP 星座表完整复现。 | 在 `## 和 3GPP 协议的关系` 增加“教学 Gray 映射与协议映射复核状态”的对照行。 |
| `docs/L1_基础/T2.14_QAM_Max_Log_MAP_demapping.md` | 16/64/256QAM 映射、精确 bit LLR 与 Max-Log-MAP；为何近似/查表/最近距离；16QAM 手算和符号反转失败。 | 能计算 16QAM 近似 bit LLR 并解释高阶 QAM 复杂度增长。 | `## 协议定位`、`## 16QAM 星座的一维 PAM 分解`、`## 精确逐比特 LLR 的复杂度来源`、`## Max-Log-MAP`、`## 16QAM 手算例子`、`## 常见错误`。 | 失败案例主要在常见错误中呈现，标题未显式点名“LLR 符号反转失败案例”。 | 增加一个三级标题 `### LLR 符号反转失败案例`，放入现有错误分析和观测现象。 |
| `docs/L1_基础/T2.15_fading_channel_LLR_reliability.md` | Rayleigh/Rician 衰落、均衡输出、信道增益改变 LLR 可靠度；不展开信道估计；单抽头例子。 | 能解释低信道增益均衡符号应有更小 LLR 幅度。 | `## 瑞利衰落与莱斯衰落`、`## 单抽头模型的核心关系`、`## 均衡会把噪声一起放大`、`## BPSK 衰落下的 LLR 推导`、`## 数值例子`。 | 协议定位提到参考信号与状态获取，可能略超出“译码器输入视角”。 | 在协议定位段末加一句“本节不教学信道估计，只使用均衡器输出可靠度”。 |
| `docs/L1_基础/T2.16_LLR_clipping_scaling_quantization.md` | LLR 裁剪、缩放、量化、饱和；定点译码器不能无限精度；过度自信、缩放不足、符号错误。 | 能解释 LLR 饱和改变译码行为并识别符号约定 bug。 | `## 浮点范围裁剪`、`## 缩放和量化`、`## 手算例子：6-bit LLR 量化`、`### 过度自信 LLR`、`### 缩放不足 LLR`、`### 符号错误`、`## 定点接口契约`。 | 与 HARQ 合并饱和的接口已有工程用例，但没有反向引用 T7.3。 | 在工程用例中补一句“T7.3 将把该饱和加法用于 HARQ soft buffer 更新”。 |
| `docs/L1_基础/T3.1_LTE_NR_CRC_families.md` | LTE/NR CRC 家族；CRC 目的、生成多项式、TB/CB/控制 CRC；必须复现已核验多项式，缺失标待核验；可执行小例子。 | 能区分 TB CRC、CB CRC 和控制 CRC 在 pass/fail 中的角色。 | `## 多层 CRC 的协议动机`、`## 协议定位`、`## LTE 协议 CRC 生成多项式复现`、`## CRC-4 教学算例`、`## Python 校验片段`。 | NR CRC 多项式若仍受抽取件限制，需在正文显式标出哪些已核验、哪些待核验；当前标题偏重 LTE 复现。 | 在协议证据表新增“NR CRC 多项式当前抽取状态/关闭条件”行，避免标题承诺大于证据。 |
| `docs/L1_基础/T3.2_transport_code_block_filler_bits.md` | TB、CB、分段、filler；为什么大 TB 拆 CB；并列 LTE/NR；小型分段例子。 | 能解释 decoder per CB 工作但最终 pass/fail per TB。 | `## 协议定位`、`## TB 与 CB 的工程动机`、`## LTE 分段规则`、`## NR 分段规则`、`## Filler Bit 的特殊语义`、`## 教学分段例子`。 | 小型例子是教学化，未对 LTE/NR 两种真实分段流程分别给最小反例。 | 增加一个“LTE/NR 差异速查”小表，标出 Turbo/LDPC/Polar 的 filler 与 CRC 位置差异。 |
| `docs/L1_基础/T3.3_LTE_Turbo_segmentation_rules.md` | LTE Turbo 专用 CB 分段与 CB CRC；最大码块、filler、并行译码；接收侧视角。 | 能将一个 LTE TB size 映射到 CB 数并识别 CB CRC 检查位置。 | `## 从协议规则到尺寸计算`、`### 合法 Turbo 块长集合`、`### 两种码块长度与填充位`、`## 手算例子`、`## 接收端并行译码流程`。 | 与 T7.4 要求的 `$K_-` 在前、`$K_+` 在后、第 0 个 CB 开头 filler 规则虽有证据，但可加显式“供 T7.4 复用”的锚点。 | 在 `### 码块长度顺序与比特填入顺序` 末尾加“后续 T7.4 重组必须按此顺序反向处理”的提示。 |
| `docs/L1_基础/T3.4_NR_LDPC_segmentation_rules.md` | NR LDPC 分段、基图最大块长、提升大小交互、CB CRC；基图选择影响分段的小例子。 | 能解释 TB size、CB、base graph、lifting size、CB CRC 的关系。 | `## 基图选择的因果关系`、`## 分段尺寸公式`、`## Lifting Size 集合复现`、`## 基图移位表复现`、`## 手算例子一/二`、`## 协议证据表`。 | 已有大量协议复现，需确保图片/表格脚本输入与本地证据路径在正文证据记录中可追溯。 | 在 `## 执行与证据记录` 补充每个资产的生成脚本、输入 CSV/HTML、输出 PNG 路径三列。 |
| `docs/L1_基础/T3.5_NR_Polar_segmentation_crc.md` | NR Polar 控制信息分段与 CRC；控制负载不同于 LDPC TB；覆盖 PUCCH/PUSCH UCI 和 DCI 背景；不扩展控制信道。 | 能解释 Polar 控制信息何时加 CRC 以及 CRC 如何支持 SCL 路径选择。 | `## 控制信息链路的工程动机`、`## NR Polar 控制信息的协议链路`、`## UCI 的 Polar 分段与 CRC 附加`、`## DCI 的 24 bit CRC 与 RNTI 加扰`、`## CRC 辅助 SCL 路径选择`。 | UCI/DCI 背景较完整，但应继续避免扩展成 PUCCH/PDCCH 物理信道课程。 | 在协议链路段增加范围声明：“仅保留影响 Polar 编码/译码输入输出的控制信息字段”。 |
| `docs/L1_基础/T4.1_iterative_decoding_extrinsic_information.md` | 迭代译码、本征信息、外信息；Turbo/LDPC 多次迭代修正置信度；两个校验关系例子；不默认图论。 | 能区分 channel LLR、a priori、extrinsic、posterior LLR。 | `## 四类信息的定义`、`## 两个校验关系的手算例子`、`## 外信息的非回声原则`、`## Turbo 译码中的外信息交换`、`## LDPC 译码中的消息传递`。 | Polar 不属于迭代外信息主线，正文未明确“不强行归入外信息交换”。 | 在三类译码对比处增加一句“Polar SCL 主要是路径列表搜索，不按本节外信息迭代模型教学”。 |
| `docs/L1_基础/T4.2_graphs_trellises_trees_for_decoding.md` | 因子图、Tanner 图、网格图；对比 Turbo 网格、LDPC 图、Polar 树；要求 Mermaid。 | 能识别 Turbo/LDPC/Polar 分别属于哪种图模型。 | `## 因子图的通用含义`、`## Tanner 图与 LDPC 的手算例子`、`## 网格图与 Turbo`、`## Polar 树与路径`、`## 三类图的对比`。 | Mermaid 图存在但协议 Figure/Table 仍作为背景锚点，未复现协议图表。 | 在协议证据表中把“算法示意图”和“3GPP 原图/表复现状态”分列。 |
| `docs/L1_基础/T4.3_HARQ_soft_combining_basics.md` | HARQ、RV、软缓存、Chase 合并、增量冗余；跨重传累积 LLR；软合并数值例子。 | 能解释 retransmission LLRs 被相加或放入 circular-buffer-derived soft buffer。 | `## HARQ 的基本角色`、`## Chase 合并与增量冗余`、`## LLR 软合并的数值例子`、`## 协议中的 RV 与 circular buffer`、`## 接收端流程`。 | 未复现完整 RV 表、MCS/TBS 表和 bit-exact rate recovery，正文已有未覆盖声明。 | 保持当前未覆盖声明，并在 T7.3/T9.3 完成后反向补“详见”链接，避免基础课重复协议细节。 |
| `docs/L1_基础/T4.4_early_stopping_crc_gated_control.md` | 讲解早停、CRC 门控、LDPC syndrome/CRC、Polar CRC-aided selection、CRC 误通过、stop controller、工程计数器、CRC latency、pipeline flush、状态机和协议边界。 | 能解释早停是实现策略，CRC 是检测不是纠错，并能设计最小 stop controller。 | `## 早停控制的工程动机`、`## CRC 是错误检测，不是纠错`、`## Turbo、LDPC 与 Polar 的停止条件对比`、`## CRC 误通过`、`## Stop Controller 伪代码`、`## RTL/ASIC 控制映射`、`## 验证方法`、`## 协议证据表`。 | 无 Critical/Important 缺口；审核经理指出的 `candidate_valid`、CRC latency 和 pipeline flush 已修复。引用重建输出为候选清单。 | 后续 L3 可把 stop controller 接入真实 golden/RTL 回归，验证 candidate tag、flush 和 HARQ 提交边界。 |
| `docs/L1_基础/T4.5_decoder_performance_metrics.md` | 定义 BER、BLER、FER、吞吐、延迟、迭代、每比特能耗、面积吞吐；讲 BLER vs $E_b/N_0$、统计置信、错误块停止、协议边界和工程取舍。 | 能设计 BLER 曲线仿真，并说明哪些指标是工程统计而非 3GPP 强制门限。 | `## 统计对象、分母、单位与公式`、`## BLER vs $E_b/N_0$ 曲线`、`## 统计置信度`、`## 吞吐、延迟、迭代和能耗`、`## 浮点仿真方案`、`## RTL/ASIC 计数器映射`、`## 协议证据表`。 | 无 Critical/Important 缺口；审核经理只提出执行记录计划语气，已改为实际审计结果。引用重建输出为候选清单。 | L3 仿真阶段应把本节 CSV 字段、错误块停止条件和置信区间说明落实为可执行脚本。 |
| `docs/L1_基础/T4.6_decoder_interface_contracts.md` | 定义公共 decoder descriptor、input/output/status/error/debug，说明 LLR 符号、TB/CB/HARQ/RV soft buffer key、三类译码器映射、字段检查、反压、复位、超时、定点、RTL/ASIC 和验证。 | 能设计 Turbo/LDPC/Polar 共用接口契约，并说明协议语义和实现字段边界。 | `## 中立接口总览`、`## 公共译码器描述符表`、`## 公共 Input/Output/Status/Error/Debug 表`、`## Turbo、LDPC 与 Polar 映射关系`、`## HARQ Process ID、RV 与 Soft Buffer Key`、`## 接收端流程`、`## 伪代码`、`## RTL/ASIC 接口映射`。 | 无 Critical/Important 缺口；审核经理指出的 `llr_sign_convention` 检查和 RV/soft buffer key 表述已修复。TS 36.213 精确分册保留待核验，不伪造锚点。 | T17.1 可直接继承本节 descriptor/status/error/debug 契约，进入 golden model 与 bit-exact 接口设计。 |
| `docs/L1_基础/T5.1_fixed_point_numbers_for_LLR.md` | 有符号定点、补码、Q 格式、饱和、舍入、裁剪；Q 格式例子和 Python 位级检查。 | 能编码/解码 signed fixed-point LLR 并解释 saturation。 | `## 二进制补码`、`## Q 格式`、`## 手算例子`、`## 量化误差、舍入和截断`、`## 裁剪与饱和`、`## Python 位级检查`。 | HARQ 软合并饱和有例子，但没给 T7.3 的 circular buffer 场景引用。 | 在 `## 裁剪与饱和` 的软合并示例后补“该模式用于 T7.3 HARQ soft buffer”。 |
| `docs/L1_基础/T5.2_memory_banking_buffering_basics.md` | SRAM、寄存器文件、乒乓缓存、循环缓存、bank、bank 冲突；预览 Turbo/LDPC/Polar 存储。 | 能解释并行译码为何需要 banked memory 并识别 bank conflict。 | `## 存储器的基本对象`、`## 分 bank 的基本映射`、`## 手算例子：bank 冲突判断`、`## 乒乓缓存`、`## 循环缓存`、`## 协议背景到存储需求的翻译`。 | Turbo/LDPC/Polar 预览是高层映射，未给每类一个最小地址序列例子。 | 增加一个 3 行表：Turbo interleaver、LDPC layered、Polar path memory 各给一个地址访问模式和冲突风险。 |
| `docs/L1_基础/T5.3_throughput_latency_parallelism.md` | 吞吐、延迟、启动间隔、时钟、并行度、迭代次数；清晰公式；Turbo/LDPC/Polar 高层例子。 | 能由 block size、cycles、iterations、clock 估计 bps。 | `## 基本定义`、`## 手算例子：由周期数估算吞吐`、`## 并行度的收益与限制`、`## 迭代次数与平均吞吐`、`## 三类译码器的瓶颈直觉`。 | 公式具备，协议 processing/HARQ context 锚点仍待核验。 | 在协议证据表中标明 TS 38.214 仅为后续 processing/HARQ 背景，当前不引用数值时限。 |
| `docs/L1_基础/T5.4_RTL_state_machine_handshake_basics.md` | RTL 接口、valid/ready、FSM、复位、时钟域；Mermaid stateDiagram-v2；SystemVerilog 风格接口。 | 能画 idle/load/decode/check/output/error 的 decoder FSM。 | `## valid/ready 握手`、`## 手算例子：三周期握手`、`## 译码器控制状态机`、`## 状态转移条件`、`## SystemVerilog 接口片段`。 | 时钟域基础较简，未给跨时钟 FIFO 与译码核心接口的反例。 | 增加一个短失败案例：“异步 valid 直连导致偶发丢块”，并说明本节只做基础提醒。 |
| `docs/L1_基础/T5.5_decoder_hardware_verification_mindset.md` | golden model、bit-exact、约束随机、边界、覆盖率、回归、波形；CRC pass/fail 可观察。 | 能列出 code block decoder 最小验证计划。 | `## golden model 的角色`、`## bit-exact、容差和 pass/fail`、`## 最小验证计划`、`## 约束随机与边界案例`、`## 覆盖率`、`## 波形调试`。 | CRC pass/fail 有覆盖，但未把 TB/CB 两层 CRC 分别映射到验证检查点。 | 在最小验证计划表增加 `CB CRC` 和 `TB CRC` 两个独立观察点。 |

## L2 覆盖矩阵

| 讲义 | Roadmap Prompt 要求 | Roadmap 验收要求 | 正文中已有证据位置 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|:---|
| `docs/L2_协议算法/T6.1_LTE_Turbo_decoder_chain_overview.md` | 从 demapper LLR 到解速率匹配、解交织、Turbo、CB CRC、拼接、TB CRC；区分 filler、`<NULL>`、未发送位置；接收端逆操作和协议证据。 | 能画 LTE Turbo decoding chain 并命名输入输出。 | `## 协议链路在接收端的位置`、`## 发送端顺序与接收端逆序`、`## 三类空洞的定义和后果`、`## Turbo 译码器的输入输出契约`。 | 三类空洞已覆盖，但未把 T3.3/T7.2/T7.1 的具体互链列成索引。 | 在三类空洞表中给每类增加“详见讲义”列：T3.3、T7.2、T7.1/T7.3。 |
| `docs/L2_协议算法/T6.2_RSC_code_foundation.md` | RSC 状态、移位寄存器、生成多项式直觉、系统/校验比特、网格图；先玩具例子再接 LTE。 | 能 trace 简单 RSC encoder 状态转移和 parity 输出。 | `## 玩具 RSC 结构`、`## 手算例子`、`## 状态转移和网格图`、`## 生成多项式的直觉`、`## LTE Turbo 的连接`。 | LTE Figure 5.1.3-2 media 仍待核验复现。 | 在协议证据表加“Figure 5.1.3-2 未图片化复现，当前用玩具 RSC 图讲算法”。 |
| `docs/L2_协议算法/T6.3_LTE_Turbo_encoder_trellis_termination.md` | LTE Turbo 编码器结构、两个 8 状态编码器、1/3 三路输出、内部交织器、网格终止；译码器角度讲尾比特。 | 能识别 systematic、parity-1、parity-2、tail-bit streams。 | `## 编码器结构的协议含义`、`## 三路输出和 1/3 码率`、`## $D=K+4$ 的解释`、`## 网格终止的动机`、`## 尾比特在三路流中的接收端后果`。 | 8 状态的状态编号/转移图未完整按协议图复现。 | 增加一个“协议图待核验，当前只复现三路流和尾比特语义”的证据边界行。 |
| `docs/L2_协议算法/T6.4_LTE_Turbo_internal_interleaver.md` | LTE Turbo 内部交织器、分集、参数随块长、交织/解交织地址；小交织例子；复现参数前核验 Table 5.1.3-3。 | 能解释 interleaver/deinterleaver address maps 关系。 | `## 协议表完整重建图`、`## Table 5.1.3-3 的字段含义`、`## 内部交织器地址公式`、`## 手算例子`、`## 接收端地址生成流程`。 | 参数表图已出现，但报告无法确认图生成脚本和输入数据在讲义中完整列出。 | 在执行与证据记录中补全 Table 5.1.3-3 的输入 CSV/HTML、生成脚本、输出图片路径。 |
| `docs/L2_协议算法/T6.5_BCJR_MAP_decoding_intuition.md` | alpha、beta、gamma、posterior LLR；小网格直觉再严谨推导。 | 能描述 alpha/beta/gamma 及其在 bit posterior 中的作用。 | `## 网格模型与三类度量`、`## 分支度量的概率含义`、`## 前向度量递推`、`## 后向度量递推`、`## 后验 LLR 的网格求和`、`## 手算例子`。 | 公式推导充分，但与 T6.6 的 log-domain 过渡可更显式。 | 在末尾补一段“数值下溢问题引出 T6.6 对数域实现”。 |
| `docs/L2_协议算法/T6.6_Log_MAP_Max_Log_MAP_Turbo.md` | MAP 到对数域；max-star 修正；Max-Log-MAP 近似及性能/复杂度；max-star 数值例子。 | 能解释 Max-Log-MAP 更简单以及丢掉的 correction。 | `## 概率域 MAP 的实现痛点`、`## 对数域度量定义`、`## max-star 运算`、`## 手算例子：max-star 修正项`、`## Log-MAP 递推方程`、`## Max-Log-MAP 近似`。 | 性能/复杂度取舍有文字，缺少一个“硬件资源影响”的小型对照表。 | 在 `## 性能与复杂度取舍` 增加 max-star 查表/比较器/加法器的资源对照。 |
| `docs/L2_协议算法/T6.7_Turbo_iteration_extrinsic_stopping.md` | 两个 SISO、外信息交织/解交织、迭代调度、后验判决、CRC 早停、最大迭代；Mermaid。 | 能 trace 一轮 Turbo iteration 并识别 CRC early stopping 位置。 | `## 双 SISO 译码器结构`、`## 一轮迭代的信号流程`、`## 教学例子`、`## 后验判决与 CRC 早停`、`## 多码块停止策略`。 | 最大迭代限制有概念，但未给伪代码参数名如 `max_iter`/`crc_stop_enable` 的接口化定义。 | 在伪代码输入列表中增加 `max_iter`、`crc_stop_enable`、`cb_crc_status`。 |
| `docs/L2_协议算法/T6.8_LTE_Turbo_decoder_numeric_walkthrough.md` | 缩短状态 toy Turbo-like 数值走读；分支度量、外信息、交织交换、硬判决；标注非规范 LTE 向量。 | 能跟随 toy iteration 并解释每个数字含义。 | `## 教学模型设定`、`## 输入软信息`、`## 第一译码器分支度量`、`## 第一译码器局部后验与外信息`、`## 第二译码器输出与解交织`、`## 硬判决与 CRC 检查位置`。 | 非规范向量声明存在于资料边界，但可在数值例子开头更醒目。 | 在 `## 教学模型设定` 首段加粗声明“不是 LTE 一致性测试向量”。 |
| `docs/L2_协议算法/T7.1_LTE_Turbo_de_rate_matching_overview.md` | LTE 解速率匹配；三路流经交织、循环缓存、bit collection、puncturing/repetition/RV；接收端反填三路；未发送位置是未知/擦除；接收端伪代码和小循环缓存例子。 | 能解释 received LLR 放回三路 Turbo streams，并区分 punctured/unknown 与 filler。 | `## 协议定位与接收链路`、`## 速率匹配的核心概念`、`## 数学定义与推导`、`## 数值例子：小型循环缓存反映射`、`## 接收端逆流程`。 | 子块交织细节和 `<NULL>` 处理留给 T7.2，当前总览可加显式跳转。 | 在循环缓存例子后加“本节不展开列置换和 `<NULL>` 删除，详见 T7.2”。 |
| `docs/L2_协议算法/T7.2_LTE_subblock_deinterleaver_circular_buffer.md` | 子块交织反操作、矩阵行列写入、列置换、`<NULL>`、循环缓存索引、解交织；对比 filler/`<NULL>`/未接收 LLR；小矩阵和列置换错误案例。 | 能 invert toy sub-block interleaver、识别 null，并说明 null 不同于 filler。 | `## 子块交织器的结构`、`## 32 列置换表的含义`、`## 数学定义与推导`、`## 数值例子：小矩阵交织与反交织`、`## 三种空洞的工程对比`、`## 列置换错误失败案例`。 | 32 列置换表含义有讲，但完整 Table 5.1.4-1 的协议复现状态需要明确。 | 在协议证据表标明 Table 5.1.4-1 是已核验、已复现、还是仅定位，附关闭条件。 |
| `docs/L2_协议算法/T7.3_LTE_HARQ_soft_buffer_RV.md` | LTE HARQ soft buffer size、RV、incremental redundancy、retransmission LLR 更新；RV 是 circular buffer 不同起点/区域；重传补充 punctured/未发送校验；同位置 LLR 累加/饱和；小型循环缓存合并例子；定点饱和。 | 能描述 RV 改变 selected circular-buffer region、重传覆盖 punctured/unknown、combining 改变 LLR reliability。 | 详见下一节 T7.3 专项审计。 | DL-SCH/PCH 主线 `N_cb`、`N_IR`、`E`、`k_0` 和 `<NULL>` 跳过语义已重建；TS 36.213/36.321 HARQ 过程控制锚点仍是系统边界。 | 后续若扩展调度章节，再定位 TS 36.213/36.321 的 HARQ process、RV 来源、ACK/NACK 时序；T7.3 本节不把这些边界误写成 TS 36.212 结论。 |
| `docs/L2_协议算法/T7.4_LTE_code_block_reassembly_TB_CRC.md` | CB CRC、filler/CB CRC 去除、按 r 拼接、TB CRC/ACK-NACK；承接 T3.3 `$K_-`/`$K_+`/CB0 filler；未发送位置不能在重组阶段当 filler。 | 能描述 final LTE TB pass/fail logic from decoded CBs。 | `## 两层 CRC 的职责边界`、`## 接收端重组的对象`、`## 三种空洞的边界`、`## 数值例子：两个 CB 的重组`、`## 接收端流程`。 | 三个要求案例中“全 CB 通过但 TB CRC 失败”可确认度较低，可能只在常见错误/思考题覆盖。 | 增加独立小节 `## 失败案例`，列出单 CB 失败、全 CB 通过但 TB CRC 失败、filler 处理错误三行。 |
| `docs/L2_协议算法/T7.5_LTE_DL_UL_decoding_differences.md` | 接收机视角对比 LTE DL-SCH/UL-SCH：信道路径、HARQ 时序/配置、CB 处理、TS 36.213/36.321 边界；不展开调度。 | 能解释 LTE DL/UL decoder 需要哪些 MAC/PHY control 参数。 | `## 接收机视角的差异`、`## 最小例子`、`## TS 36.212 中的共同骨架`、`## 译码器需要的参数`、`## 接收端流程对比`。 | TS 36.213/36.321 精确锚点仍待核验。 | 在协议证据表中列出待核验锚点的具体关闭动作：定位 HARQ process、RV、ACK/NACK 边界章节。 |
| `docs/L2_协议算法/T7.6_LTE_Turbo_decoder_edge_cases.md` | 小块、filler、`<NULL>`、puncturing、repetition、最大 CB 数、软缓存限制、RV 不匹配、LLR 符号不匹配、CRC 误通过、超时；诊断步骤；典型失败模式。 | 能用 checklist debug failing LTE Turbo decode。 | `## 边界案例总表`、`## 三种空洞的诊断规则`、`## 诊断流程`、`## 数值例子：LLR 符号错误`、`## 典型失败模式`、`## Python 诊断片段`。 | “最大码块数”和“软缓存限制”的诊断动作可更具体，当前可能只在表中简述。 | 在边界案例总表中为最大 CB 数、软缓存限制各增加“检查变量/期望日志/失败现象”三列内容。 |
| `docs/L2_协议算法/T8.0_TS38212_chapter5_decoder_side_map.md` | 新增 TS 38.212 Chapter 5 接收侧地图，集中讲 CRC、CB segmentation、channel coding、rate matching、code block concatenation，并把发射侧步骤逐项翻译为接收侧逆流程。 | T3.1/T3.4/T3.5/T8/T9/T10/T11 能回链到本地图；small block lengths 有明确边界。 | `## Chapter 5 的接收侧读法`、`## 发射侧步骤到接收侧逆流程`、`## Polar、LDPC 和 small block 边界`、`## 协议证据表`、`## 执行与证据记录`。 | 无阻塞缺口；本节是协议地图，不替代 T8/T9/T10 的算法推导和完整表格复现。 | 后续涉及 TS 38.212 Chapter 5 的新增讲义应回链本节，避免重复解释总流程。 |
| `docs/L2_协议算法/T8.1_NR_LDPC_decoder_chain_overview.md` | NR LDPC 接收链路总览；demapper LLR、rate recovery、LDPC decoder、CB CRC、CB concat、TB CRC、HARQ feedback；解释 LDPC 为什么用于 NR 数据业务；给 descriptor 和接收流程。 | 能画 NR LDPC decoding chain 并命名输入输出。 | `## LDPC 名称、来源和 NR 数据业务动机`、`## 接收端链路总览`、`## 最小 descriptor v0`、`## 两个 CB 的教学接收流程`、`## 接收端伪代码`、`## 协议证据表`。 | TS 38.214 MCS/TBS/RV/CBG 表未在本节复现，正文已说明只作为背景，具体表值留到 T9/T11。 | 模块 9/11 使用 TS 38.214 具体表值时重建相关表格子集。 |
| `docs/L2_协议算法/T8.2_NR_LDPC_base_graph_selection.md` | NR LDPC BG1/BG2 选择条件；解释 BG 是模板不是完整 H；讲 A、R、K_b 和 TS 38.214 中 R 的来源；复现协议选择逻辑并给边界例子。 | 能按 A、R 判断 BG1/BG2，并说明 BG2 的低码率/短块动机。 | `## 协议规则的完整复现`、`## 为什么需要两个基图`、`## $A$、$R$ 与方向上下文`、`## 接收端 descriptor`、`## 典型边界输入`、`## 协议证据表`。 | §6.2.2/§7.2.2 公式在 `content.md` 中抽取不完整，已用 Word XML/media 证据链和人工转写关闭。 | 后续若升级抽取工具，可用原始公式图片或 OMML 复核人工转写。 |
| `docs/L2_协议算法/T8.3_NR_LDPC_lifting_QC_matrix.md` | 讲 lifting size、set index、QC-LDPC、基矩阵到完整 H 的构造；复现 TS 38.212 Table 5.3.2-1/2/3；解释每列含义；给 toy QC 展开。 | 能由 BG、Zc、iLS、shift value 解释完整 H 的子矩阵构造。 | `## Table 5.3.2-1 的本地复现`、`## Table 5.3.2-2/3 的本地复现与列含义`、`## 基图表与完整矩阵的层级`、`## toy QC 展开例子`、`## 地址生成与 RTL 映射`。 | 无阻塞缺口；长表采用图片化完整复现而非 Markdown 全表，已记录输入 CSV/HTML、脚本、SHA-256 和输出图片。 | 后续引用 Table 5.3.2-2/3 时复用本节资产并解释本节使用的列/行/shift 语义。 |
| `docs/L2_协议算法/T8.4_LDPC_Tanner_graph_message_passing.md` | 解释 Tanner 图、变量节点、校验节点、边、syndrome；连接 TS 38.212 的 H；说明系统位、校验位、punctured/filler LLR 语义；给消息流、伪代码和 syndrome 例子。 | 能把 H 画成 Tanner 图，并计算一个 hard decision 的 syndrome。 | `## Tanner 图的含义`、`## syndrome 的定义`、`## GF(2) 手算例子`、`## 系统位、校验位、打孔位和 filler bits`、`## 迭代译码接收端流程`、`## Python syndrome 校验片段`。 | 已修复“列空间”误导性表述为完整 H 列位置/母码位置；无剩余阻塞缺口。 | 模块 9 rate recovery 继续细化 unknown/shortened/repetition 的 LLR 放置规则。 |
| `docs/L2_协议算法/T8.5_LDPC_sum_product_BP.md` | 推导 LDPC BP/SPA：VN 更新、CN 更新、posterior LLR、syndrome early stop；讲外信息、tanh/atanh、最大迭代、CRC 边界；与 Turbo 对比。 | 能写出 SPA 变量节点/校验节点公式，并解释 syndrome 与 CRC 边界。 | `## BP 和 SPA 的基本含义`、`## 变量节点更新推导`、`## 校验节点更新推导`、`## toy BP 数值例子`、`## syndrome early stop 与 CRC 边界`、`## Turbo BCJR/MAP 与 LDPC BP 对比`。 | 已修复一轮 VN 更新闭环和 §5.4.2.1 rate recovery 实现推论边界；无剩余阻塞缺口。 | T8.6/T8.7 已承接复杂度近似和调度实现。 |
| `docs/L2_协议算法/T8.6_LDPC_MS_NMS_OMS.md` | 讲 Min-Sum 近似、符号乘积、最小值/次小值、Normalized Min-Sum、Offset Min-Sum、性能复杂度取舍、check-node 数值例子。 | 能用同一组输入计算 MS/NMS/OMS 并说明硬件取舍。 | `## 从 SPA 到 Min-Sum`、`## 符号规则`、`## min1/min2 机制`、`## 归一化 Min-Sum`、`## 偏移 Min-Sum`、`## 同一组输入的数值例子`、`## RTL/ASIC 映射`。 | 无阻塞缺口；审核经理无 Important，Minor 已补并列最小值 tie-break 和来源检查。 | 后续模块 10 可复用本节定点饱和和 min1/min2 验证字段。 |
| `docs/L2_协议算法/T8.7_layered_LDPC_decoding_schedule.md` | 讲 flooding 与 layered 调度、layered 收敛更快、QC 行映射到层、消息存储变化、小型 layered 更新表、syndrome/CRC early stop 边界和硬件取舍。 | 能区分 flooding/layered schedule 并说明硬件 tradeoff。 | `## 两种调度的时序差异`、`## layered 收敛更快的原因`、`## layer 与 base graph row group`、`## 小型 H 的 layered 更新例子`、`## 消息存储和 read-modify-write`、`## syndrome early stop 与 CRC early stop`。 | 已修复 CB CRC 条件性、syndrome 非单调、row order 协议边界和日志字段；无剩余阻塞缺口。 | 模块 10 可继续细化 bank conflict、pipeline stall 和 layer parallelism。 |
| `docs/L2_协议算法/T8.8_NR_LDPC_decoder_numeric_walkthrough.md` | 玩具 LDPC 数值走读：channel LLR、H、CN 更新、VN 更新、hard decision、syndrome、early stop；明确教学例子不是 NR 向量；给 Python、定点/RTL/验证。 | 能完成一轮 toy Min-Sum iteration 并检查 syndrome。 | `## 输入：toy H 与 channel LLR`、`## 初始 hard decision`、`## 初始 syndrome 计算`、`## 一轮 Min-Sum CN 更新`、`## VN posterior LLR 合成`、`## 更新后的 hard decision 与 syndrome`、`## Python 可复现校验`。 | 已修复“列空间”术语、CRC mask 语境、CN 公式 q 消息边界和图片拆行；无剩余阻塞缺口。 | 后续 T9/T10 使用本节 toy 数值作为 LDPC core 局部 directed test。 |
| `docs/L2_协议算法/T9.0_TS38214_MCS_TBS_decoder_descriptor.md` | 新增 TS 38.214 MCS/TBS 到译码 descriptor 专题，讲 MCS、Qm、目标码率、PRB、RE、层数、TBS 如何影响译码器输入规模，并核验 1024QAM 表号和 descriptor 字段分类。 | TS 38.214 表号、MCS 行和本地路径已核验；输出 descriptor 字段表，区分译码器输入字段和系统调度元数据。 | `## 从调度表到译码 descriptor`、`## MCS、Qm、目标码率和 TBS 链路`、`## 1024QAM 核验证据`、`## descriptor 字段分类`、`## Rel-19 最大规模压力例子`、`## 执行与证据记录`。 | 无阻塞缺口；当前仍不声称完整 MCS/TBS bit-exact 查表闭环，真实 conformance 向量阶段需按实际使用范围复现表格子集。 | 系统级向量阶段按本节字段表生成 bit-exact descriptor，并复现实际使用的 TS 38.214 表格子集。 |
| `docs/L2_协议算法/T9.1_NR_LDPC_rate_recovery_overview.md` | NR LDPC 接收侧速率匹配反操作：比特选择反操作、循环缓存、冗余版本、比特解交织、有限缓存速率匹配和软合并。 | Learner can explain where received LLRs are inserted in the LDPC soft buffer. | `## 协议前因后果`、`## 接收端对象模型`、`## 发送端与接收端的逆向关系`、`## bit interleaving 的反操作`、`## bit selection 的接收端反向理解`、`## 小型 circular buffer 例子`、`## 接收端伪代码`、`## 浮点仿真方案`、`## RTL/ASIC 映射`。 | 本节按总览边界未复现 TS 38.212 Table 5.4.2.1-1/2 的具体表值，正文已说明 `content.md` 公式符号抽取不完整，具体 RV 起点表值留给 T9.3 或需要 bit-exact 表值的章节。 | T9.2 继续细化 puncturing/shortening/repetition；T9.3 使用具体 RV/CBG 表值时先重建 TS 38.212/38.214 相关表格子集。 |
| `docs/L2_协议算法/T9.2_NR_LDPC_circular_buffer_states.md` | 登记表 T9.2 要求讲解 NR LDPC circular buffer、puncturing、shortening、repetition；用同一张图对比三类状态，给数组例子、Python 校验、错误对照、定点饱和和 RTL 映射。 | 能区分 punctured/unknown、shortened/filler、repeated，并说明三类位置的 LLR 初始化或合并方式。 | `## 术语来源与常见误解`、`## circular buffer 的接收端含义`、`## Puncturing 与 unknown LLR`、`## Shortening 与 known mask`、`## Repetition 与 LLR 累加`、`## 三类状态图`、`## 小型数组例子`、`## Python 可复现校验`、`## RTL/ASIC 映射`。 | roadmap T9.2 与登记表 T9.2/T9.4 存在命名错位；正文已说明本节按登记表执行，bit deinterleaving 留给 T9.4。TS 38.212 Table 5.4.2.1-1/2 具体值仍留给 T9.3。 | T9.3 继续展开 HARQ soft buffer、RV、CBG 和具体 RV 起点/部分重传状态；T9.4 按 roadmap 原意展开 Qm bit deinterleaving。 |
| `docs/L2_协议算法/T9.3_NR_LDPC_HARQ_soft_buffer_RV_k0.md` | NR LDPC 冗余版本、`k0`、循环缓存起点、HARQ 软合并、有限缓存行为和重传处理；登记表扩展要求同时覆盖 CBG、CBGTI、CBGFI、partial retransmission、未重传 CBG 保持、状态字段、失败案例和最小 dump。 | Learner can explain how RV changes the selected code-bit region and why soft buffer consistency matters. | `## 协议前因后果`、`## TB、CB 与 CBG 的层级`、`## HARQ soft buffer 的接收端职责`、`## RV、k0 与 circular buffer 起点`、`## CBG partial retransmission 的接收端语义`、`## 小型 CBG mask 与 toy k0 例子`、`## 接收端伪代码`、`## 定点化策略`、`## 验证方法与最小 dump 包`。 | T9.3 已修正并复现 TS 38.212 Table 5.4.2.1-2 的 `rvid/BG/k0` 公式形式，包含 $Z_c$ 对齐项；正文已说明本地 Markdown/CSV 抽取限制。roadmap/register 对 T9.4 命名存在错位，登记表 T9.4 已转为 bit deinterleaving。 | 后续若需要独立 bit-exact 表格证据，可为 Table 5.4.2.1-2 单独生成图片化复现资产；CBG 调度边界若继续深化，应作为独立补充任务，不挤占登记表 T9.4。 |
| `docs/L2_协议算法/T9.4_NR_LDPC_bit_deinterleaving.md` | 登记表 T9.4 要求讲解 NR LDPC bit deinterleaving 与 LLR 放置：精读 TS 38.212 bit interleaving/rate matching，说明 `Qm` 如何影响 LLR 排列，讲 demapper 输出到 LDPC decoder input 的映射，给 QPSK/16QAM 例子，画流程图，讨论 permutation address generator、LLR reorder buffer、bank conflict、伪代码和常见错误。 | 能按 `Qm` 将 demapper symbol-order LLR 反交织为 rate-recovery 输入 `e` 顺序，并解释 bit order 错误为何高 SNR 仍 CRC fail。 | `## 资料与协议边界`、`## 调制阶数 Qm 与 LLR 排列关系`、`## 协议公式与接收端反操作`、`## 接收端对象模型`、`## bit deinterleaving 流程图`、`## QPSK 数值例子`、`## 16QAM 数值例子`、`## 接收端伪代码`、`## Python 可复现校验`、`## RTL/ASIC 映射`、`## 验证方法`。 | TS 38.212 `content.md` 对 §5.4.2.2 数学符号抽取不完整，正文已按协议算法重写等价索引式并用固定 pattern Python 校验；roadmap T9.4 与登记表 T9.4 命名错位已在正文说明。 | T9.5 继续码块处理、CB CRC、TB CRC 和 CBG 部分重传后的 CB 状态使用；T9.6 汇总 NR LDPC 边界案例。 |
| `docs/L2_协议算法/T9.5_NR_LDPC_reassembly_TB_CRC.md` | 路线图/登记表 T9.5 要求讲解 NR LDPC 解码后码块处理：filler 去除、CB CRC、码块拼接、TB CRC 和通过/失败上报；包含 CB 失败、CBG 部分重传和 TB CRC 失败案例；登记表还要求说明 CB 按协议顺序拼接、未更新 CB 状态使用、Python 验证和 HARQ feedback 区分。 | Learner can define final NR LDPC TB pass/fail logic and CBG interaction. | `## 资料与协议边界`、`## 协议前因后果`、`## Filler、CB CRC 与 TB CRC 的接收端边界`、`## CB CRC 的条件性职责`、`## 按协议顺序拼接`、`## 手算例子：两个 CB 的最小重组`、`## CBG 部分重传下的重组状态`、`## TB CRC 与 HARQ 反馈`、`## 失败案例一/二/三`、`## Python 可复现校验`、`## RTL/ASIC 映射`、`## 验证方法与最小 dump 包`。 | 真实 CRC 多项式计算不在本节重复实现，正文已说明由 T3.1 复现；本节 Python toy 专注验证输入边界、filler/CB CRC 去除和拼接顺序。 | T9.6 继续汇总 NR LDPC 端到端边界案例，特别是 syndrome pass but CRC fail、CBG mismatch、CRC 通过但上层组包失败等调试路径。 |
| `docs/L2_协议算法/T9.6_NR_LDPC_decoder_edge_cases.md` | 登记表 T9.6 要求讲解 NR LDPC 译码边界案例：BG 选择、Zc、filler、punctured systematic bits、limited buffer、RV mismatch、CBG mismatch、LLR saturation、syndrome/CRC 不一致、CRC 通过但上层组包失败；每个案例列触发条件、现象、必查字段、定位步骤、修复方向；给最小 dump 包和 descriptor->rate recovery->LDPC core->CRC 诊断流程图。 | 能用 checklist 排查 NR LDPC 失败，并解释为什么不应优先怀疑 LDPC core。 | `## 排查顺序原则`、`## 边界案例的理论模型`、`## 诊断变量的基础解释`、`## 现象到假设的推理方式`、`## 边界错误的传播路径`、`## 直观解释：同一个 CRC fail 背后的四类坐标系`、`## 数值例子：RV 地址错位如何污染软缓存`、`## 理论推导：LLR 饱和对边界错误的放大效应`、`## 案例总表`、`## 详细案例一/二/三/四/五`、`## 最小 dump 包`、`## Python 诊断分类片段`。 | 无阻塞缺口；本节不复现全部 BG/Zc/RV 表值，正文已说明具体表值由 T8.2/T8.3/T9.3 承接，本节聚焦端到端诊断。 | 模块 10/11 继续复用本节最小 dump 包思想，分别用于 Polar 边界案例和 LTE/NR 对比诊断。 |
| `docs/L2_协议算法/T10.1_NR_Polar_decoder_chain_overview.md` | 路线图/登记表 T10.1 要求总览 NR Polar 控制信息接收侧链路：demapper LLR、rate recovery、sub-block deinterleaving、Polar decode、CRC aided selection、control output；覆盖 UCI/DCI 背景；解释 Polar 码和短控制块动机；列 descriptor；给小型控制块流程、验证方法和常见错误。 | Learner can draw NR Polar decoding chain and name UCI/DCI contexts. | `## Polar 名称、来源与短控制块动机`、`## 协议中的控制信息编码映射`、`## 信道极化的最小直觉`、`## 冻结位、信息位和 CRC 位`、`## 接收侧总链路图`、`## 发送端协议顺序与接收端逆序`、`## UCI 与 DCI 的协议分支`、`## 最小 decoder descriptor v0`、`## 小型控制块流程例子`、`## 接收端伪代码`、`## Python 可复现校验`。 | 无阻塞缺口；本节只复现 Table 5.3-2 和可靠性序列前 16 项读表示例，完整可靠性序列和 Polar rate matching 表按正文说明由 T10.3/T10.7 承接。 | T10.2 继续零基础讲信道极化、frozen/information bit 和 N=4/N=8 编码变换；T10.3/T10.7 关闭完整表格和索引规则。 |
| `docs/L2_协议算法/T10.2_channel_polarization_frozen_bits.md` | 路线图/登记表 T10.2 要求讲解信道极化、可靠/不可靠 bit-channel、冻结位、信息位和 Polar 短控制块动机；必须说明信道极化不是物理极化；给 N=4 或 N=8 极化变换图、手算生成矩阵或蝶形结构、接收端 frozen mask、Python 小例子和常见错误。 | Learner can identify frozen and information bit positions in a toy Polar code. | `## 信道极化的基础解释`、`## 最小二比特变换`、`## 冻结位和信息位`、`## N=4 极化变换图`、`## N=4 生成矩阵手算例子`、`## 接收端 frozen mask`、`## bit-reversal 与顺序约定边界`、`## Python 可复现校验`、`## 验证方法`。 | 无阻塞缺口；本节不复现完整 NR reliability sequence，也不处理 rate matching 后的 puncturing/shortening/repetition 如何改变集合，正文已说明由 T10.3/T10.7 承接。 | T10.3 继续完整核验和复现 TS 38.212 Table 5.3.1.2-1，生成 info/frozen mask；T10.4 承接 SC f/g 和 partial sum。 |
| `docs/L2_协议算法/T10.3_NR_Polar_reliability_sequence.md` | 路线图/登记表 T10.3 要求精读 TS 38.212 Table 5.3.1.2-1，解释可靠性序列、排序方向、索引基准、按 K 选择信息位、CRC 是否计入 K、toy reliability order、frozen mask、查表实现、伪代码、RTL 映射和常见错误。 | Learner can derive info/frozen mask from the NR Polar reliability sequence for a given N and K. | `## 资料与协议边界`、`## 可靠性序列的定义`、`## 排序方向和索引基准`、`## 直观解释：从可靠性理论到接收端 mask`、`## A 与 K 的边界`、`## 完整表图复现`、`## 数值例子：N=8, K=4 的可靠性筛选`、`## 数值例子：N=16, K=6 的非连续信息集合`、`## 接收端伪代码`、`## 查表实现的工程动机`、`## RTL/ASIC 映射`。 | 无阻塞缺口；完整 1024 项表已由 `table_0012.csv` 图片化复现，正文记录 CSV/HTML SHA-256、图像边界检查、目检和 Python 校验。rate matching 对集合的进一步影响按正文边界由 T10.7 承接。 | T10.4 继续展开 SC f/g、partial sum 和 N=4 译码树；T10.7 关闭 Polar rate recovery 与 puncturing/shortening/repetition 对接收端 LLR 初始化的影响。 |
| `docs/L2_协议算法/T10.4_Polar_SC_decoding.md` | 路线图/登记表 T10.4 要求推导连续消除译码，包括 f/g LLR 函数、partial sum、冻结位判决和树遍历；包含 N=4 数值例子；工程上补仿真、定点、RTL/ASIC、验证和证据记录。 | Learner can trace SC decoding decisions through a toy Polar tree. | `## SC 的译码对象`、`## 二比特节点的概率模型`、`## f 函数推导`、`## g 函数推导`、`## partial sum 的含义`、`## 判决规则`、`## N=4 树遍历图`、`## 数值例子：N=4 完整 SC 译码`、`## 接收端伪代码`、`## 浮点仿真方案`、`## 定点化策略`、`## RTL/ASIC 映射`、`## 验证方法`。 | 无阻塞缺口；本节明确 SC 是接收端基础算法而非 TS 38.212 强制实现，协议证据用于约束 $G_N=F^{\\otimes n}$、GF(2)、information/frozen 集合和输入边界。图片与 Python 校验已记录。 | T10.5 在本节基础上展开 SCL 路径分裂、path metric、排序剪枝和 list size；T10.6 继续展开 CRC-aided path selection。 |
| `docs/L2_协议算法/T10.5_Polar_SCL_decoding.md` | 路线图/登记表 T10.5 要求讲解连续消除列表译码、路径分裂、路径度量、剪枝、列表大小和复杂度；包含玩具路径度量例子，并说明排序对硬件的影响。 | Learner can explain how list decoding keeps multiple candidate paths and prunes them. | `## SCL 的译码对象`、`## 路径、分裂和剪枝`、`## LLR 域 PM 更新`、`## 列表大小 L 的工程含义`、`## 数值例子：N=4, L=2 路径表`、`## 路径分裂图`、`## 接收端伪代码`、`## 复杂度与延迟`、`## 定点化策略`、`## RTL/ASIC 映射`、`## 验证方法`。 | 无阻塞缺口；本节固定 PM 越小越好，N=4/L=2 例子逐步列出 path、PM、保留/剪枝状态，图片和 Python 校验已记录。CRC-aided 最终选择明确由 T10.6 承接。 | T10.6 继续展开 CRC-aided SCL：最佳 PM 路径 CRC fail、次优路径 CRC pass、误通过风险、list size 取舍和多路径 CRC checker。 |
| `docs/L2_协议算法/T10.6_CRC_aided_SCL_control_reliability.md` | 路线图/登记表 T10.6 要求讲解 CRC 辅助 SCL：CRC 比特如何辅助路径选择、误通过风险、列表大小取舍、控制信道低延迟和高可靠；包含最佳度量路径 CRC 失败案例；覆盖 final selector、RTL/ASIC 和常见错误。 | Learner can explain how CRC selects among SCL candidate paths. | `## 名称、来源与常见误解`、`## 接收端直观模型`、`## 控制信息的工程动机`、`## CA-SCL 的接收端流程`、`## CRC 是路径选择辅助`、`## 选择规则的理论推导`、`## UCI CRC 与 DCI RNTI 边界`、`## 数值例子：PM 最佳路径 CRC fail`、`## Final selector 图`、`## CRC 误通过风险`、`## 列表大小、低延迟和高可靠取舍`、`## Final selector 伪代码`、`## RTL/ASIC 映射`。 | 无阻塞缺口；本节精读 TS 38.212 §5.1、§5.2.1、§6.3.1.2.1、§6.3.2.2.1、§7.3.2-§7.3.4，明确 UCI 6/11 bit CRC 和 DCI 24 bit CRC/RNTI 边界；图片与 Python selector 校验已记录。 | T10.7 继续展开 Polar rate recovery；T10.8 汇总无 CRC 小负载、CRC 长度、RNTI、frozen mask、path metric 并列和 puncturing/shortening mismatch 等边界案例。 |
| `docs/L2_协议算法/T10.7_NR_Polar_rate_recovery.md` | 路线图/登记表 T10.7 要求讲解 NR Polar rate matching 的接收侧反操作：sub-block deinterleaving、bit collection reverse、puncturing、shortening、repetition、bit interleaving；给小型循环缓存例子，解释 punctured unknown、shortened strong known、repeated accumulate，画流程图，对比 LDPC/LTE Turbo 并给 Python toy。 | Learner can explain how received Polar LLRs are restored into mother-code positions before SC/SCL/CA-SCL decoding. | `## 资料与协议边界`、`## 发送端步骤与接收端反操作`、`## Sub-block interleaver pattern 复现`、`## 三类速率匹配状态的 LLR 语义`、`## Coded-bit deinterleaving`、`## 数值例子：N=8, E=10`、`## 速率恢复流程图`、`## 与 NR LDPC 和 LTE Turbo 的差异`、`## Python 可复现校验`、`## 定点化策略`、`## RTL/ASIC 映射`。 | 无阻塞缺口；TS 38.212 Table 5.4.1.1-1 已用 `table_0019.csv/html` 复现 32 项 pattern，并记录 SHA-256。已显式纠正 `table_0013` 是 LDPC lifting size set，不是 Polar pattern。图形初版说明框底部留白不足，已修复脚本并把文本框内边距/模块间距检查写入全局标准。 | T10.8 继续汇总无 CRC 小负载、CRC 长度、list exhaustion、PM tie、frozen mask、puncturing/shortening mismatch、DCI/UCI descriptor 和 RNTI/CRC 边界案例。 |
| `docs/L2_协议算法/T10.8_NR_Polar_decoder_edge_cases.md` | 路线图/登记表 T10.8 要求整理 NR Polar 译码边界案例：无 CRC 小负载、CRC 长度选择、列表大小耗尽、路径度量并列、puncturing/shortening 不匹配、冻结位 mask 错误、DCI/UCI 背景不匹配；登记表进一步要求每个案例列触发条件、现象、必查字段、定位步骤、修复建议，并给最小 dump 包和 descriptor -> rate recovery -> mask -> SC/SCL -> CRC selector 诊断流程。 | Learner can debug a failing NR Polar decode with a checklist. | `## 资料与协议边界`、`## 接收端诊断流程`、`## 案例总表`、`## 案例一：无 CRC 小负载` 到 `## 案例八：RNTI/CRC 边界错误`、`## 数值例子：frozen mask 错导致候选 CRC fail`、`## 数值例子：PM 最优路径 CRC fail 但次优路径通过`、`## 最小 dump 包`、`## Python 诊断分类片段`。 | 无阻塞缺口；本节按诊断讲义定位，精读 TS 38.212 §5.1、§5.2.1、§5.4.1、§6.3.1.2.1、§6.3.1.2.2、§6.3.2.2.1/2、§7.3.2-§7.3.4，并明确 SCL list size、PM tie-break 和 RTL sorter 属于接收端实现策略。图片已按新视觉规则检查文本框内边距、说明框底部留白、模块间距和全图几何。 | 模块 11 可复用 T10.8 的最小 dump 包思想，对比 Turbo/LDPC/Polar 的故障定位字段。 |
| `docs/L2_协议算法/T11.1_Turbo_LDPC_Polar_algorithm_comparison.md` | 路线图/登记表 T11.1 要求从图模型、迭代/列表行为、复杂度、存储、延迟和数据/控制信道适配性对比 Turbo、LDPC 与 Polar；登记表进一步要求定位 LTE Turbo 与 NR LDPC/Polar 协议链路，解释三类码名称和接收端对象，对比 trellis/Tanner graph/decoding tree、SISO/message passing/SC-SCL、软信息语义、停止条件、工程取舍，并给同一输入 LLR 的教学例子和验证方法。 | Learner can explain why LTE data used Turbo, NR data uses LDPC, and NR control uses Polar. | `## 资料与协议边界`、`## 三类码的基本思想`、`## 图模型总览`、`## 三种译码行为`、`## 同一组 LLR 的三种消费方式`、`## 协议代际与适用条件`、`## 算法行为对比表`、`## 工程取舍矩阵`、`## 接收端统一 descriptor`、`## 验证方法与最小 dump 包`。 | 无阻塞缺口；正文复现 TS 36.212 Table 5.1.3-1/2 和 TS 38.212 Table 5.3-1/2 的本节使用子集，并说明 CSV 合并单元格抽取限制。图片 `T11.1_Turbo_LDPC_Polar_algorithm_comparison.png` 已按最新全局规则修复：连线端点精确落在节点边缘，箭头尖端精确到目标边缘，边界检查和目检已记录。 | T11.2 继续对比三类 rate matching/rate recovery；T11.3 对比 HARQ soft buffer；T11.4 对比硬件架构取舍；T11.5 对比信道/信息类型到译码器家族映射。 |
| `docs/L2_协议算法/T11.2_LTE_NR_rate_matching_comparison.md` | 路线图/登记表 T11.2 要求从接收端视角对比 LTE Turbo、NR LDPC 和 NR Polar 的速率匹配/反匹配，聚焦 circular buffer、interleaving、RV 和 reverse mapping；登记表进一步要求说明共同目标、空洞语义不可混用、统一字段表、小型循环缓存例子、定点重复 LLR 累加/饱和、RTL address generator/mask RAM 可共享抽象和不可共享协议规则，并给三条并排流程图。 | Learner can state key differences in de-rate matching for Turbo, LDPC, and Polar. | `## 共同目标`、`## 三条协议链路的正向与反向关系`、`## 对比流程图`、`## 统一对象模型和字段表`、`## 不能混用的空洞语义`、`## 公式化的反向映射`、`## 小型循环缓存例子`、`## 接收端伪代码`、`## 定点化策略`、`## RTL/ASIC 映射`、`## 验证方法`。 | 无阻塞缺口；正文复现 TS 36.212 Table 5.1.4-1 和 TS 38.212 Table 5.4.1.1-1。TS 38.212 Table 5.4.2.1-2 的 `k0` 数值在当前 `table_0021.csv/html` 抽取为空，正文明确不使用具体值并记录关闭条件。图片 `T11.2_LTE_NR_rate_matching_comparison.png` 已按用户反馈修复循环缓存遮挡、表格字号偏小和表格离上方框图过近问题，脚本新增局部几何间距断言。 | T11.3 继续对比 LTE/NR HARQ soft buffer、RV ring buffer、NR CBG mask、LLR 累加和 soft buffer 生命周期；全项目图片审计需按新增规则检查表格字号和局部几何间距。 |
| `docs/L2_协议算法/T11.3_HARQ_soft_buffer_comparison.md` | 路线图/登记表 T11.3 要求对比 LTE 与 NR 的 HARQ soft buffer 处理、RV 含义、CBG 支持和译码控制影响；调度细节只保留影响译码器状态的部分。登记表进一步要求明确 soft buffer 是译码器状态，画 LTE/NR soft buffer lifecycle，给 LTE RV0/RV2 和 NR CBG partial retransmission 小例子，讨论定点饱和、saturation counter、最小 dump 包和负测试。 | Learner can explain how NR CBG changes retransmission granularity compared with LTE TB/CB flow. | `## 资料与协议边界`、`## 协议前因后果`、`## soft buffer 的协议层边界`、`## soft combining 的概率解释`、`## RV 的共同抽象和差异`、`## soft buffer 生命周期对比`、`## NR CBG 改变重传粒度`、`## 译码器 descriptor 对比`、`## LTE 小型地址/LLR 累加例子`、`## NR 小型 CBG 部分重传例子`、`## 定点化策略`、`## RTL/ASIC 映射`、`## 验证方法与最小 dump 包`。 | 无阻塞缺口；正文精读 TS 36.212/36.213/36.321 与 TS 38.212/38.214/38.321 的 HARQ/RV/CBG/soft buffer 边界。TS 38.212 Table 5.4.2.1-2 的具体 `k0` 表值不在本节使用，正文明确边界。图片 `T11.3_HARQ_soft_buffer_comparison.png` 已按新增图形规则检查：文本框和表格单元格水平/垂直居中，边界无裁切，箭头未穿框。 | T11.4 继续对比 Turbo、LDPC、Polar 硬件架构取舍；T11.5 汇总信道/信息类型到译码器家族映射。 |
| `docs/L2_协议算法/T11.4_decoder_hardware_tradeoff_comparison.md` | 路线图/登记表 T11.4 要求对比 Turbo、LDPC、Polar 硬件架构取舍，覆盖并行度、存储访问、排序、迭代/列表深度、延迟、吞吐、功耗，并给工程决策矩阵；登记表进一步要求分别覆盖 Turbo SISO/BCJR、alpha/beta、interleaver、extrinsic RAM，LDPC layered controller、CN/VN、message memory、LLR RAM、bank conflict，Polar SC/SCL tree、partial sum、path memory、sorter、CRC checker，以及共享/不可共享模块、教学周期估算、验证影响和常见错误。 | Learner can justify why LDPC is high-throughput friendly and why Polar SCL sorting is latency-sensitive. | `## 协议前因后果`、`## 三类硬件数据通路`、`### Turbo 硬件通路`、`### LDPC 硬件通路`、`### Polar SCL 硬件通路`、`## 工程决策矩阵`、`## 统一译码子系统`、`## 吞吐和周期估算示例`、`## 数值例子：教学级周期估算`、`## 定点化策略`、`## RTL/ASIC 映射`、`## 验证方法与最小 dump 包`、`## 常见错误`。 | 无阻塞缺口；本节复用 T11.1/T8.3/T10.3 已重建的协议表图证据，自绘硬件数据通路和工程决策矩阵。图片 `T11.4_decoder_hardware_tradeoff_comparison.png` 已检查文本框和表格单元格水平/垂直居中、英文不硬拆、箭头端点贴合节点边界、表格字号可读、区块间距无接触。单篇审计通过：术语、标题、深度、LaTeX；引用重建审计输出候选清单。 | T11.5 继续汇总 LTE/NR 信道和信息类型到译码器家族的协议映射，并作为模块 11 总体审查前的最后一篇。 |
| `docs/L2_协议算法/T11.5_decoder_selection_by_channel_type.md` | 路线图/登记表 T11.5 要求把 LTE/NR 信道和信息类型映射到译码器家族，覆盖 LTE 传输信道、NR UL-SCH/DL-SCH、NR UCI/DCI 和边界情况，并包含速查表。登记表进一步要求先说明协议原因，列 LTE/NR 数据/控制映射和锚点，解释 DL-SCH/UL-SCH/UCI/DCI，说明控制短块低时延高可靠和盲检背景为何影响 Polar，说明数据长块高吞吐为何影响 Turbo/LDPC，给 selector 伪代码、descriptor 字段、配置错误案例和验证方法。 | Learner can choose Turbo, LDPC, or Polar decoder for each covered LTE/NR decoding task. | `## 协议前因后果`、`## 协议映射速查表`、`## LTE 数据传输的选择`、`## NR 数据传输的选择`、`## NR 控制信息的选择`、`## 边界情况`、`## 接收端选择伪代码`、`## Descriptor 字段设计`、`## 数值例子：四个 descriptor 的分类走读`、`## 配置错误案例`、`## 验证方法与最小 dump 包`。 | 无阻塞缺口；本节复用 T11.1 已复现的 TS 36.212 Table 5.1.3-1/2 和 TS 38.212 Table 5.3-1/2，并新增自绘 selector 图。已明确 PBCH、调度器、搜索空间、PDCCH/PUCCH/PUSCH 资源映射不在本节展开。图片 `T11.5_decoder_selection_by_channel_type.png` 已目检表格/节点/说明框文字水平和垂直居中、箭头端点贴合节点边界、底部说明框留白充足。单篇审计通过：术语、标题、深度、LaTeX；引用重建审计输出候选清单。 | 模块 11 已完成 T11.1-T11.5；下一步执行 T11 全量自动审计和阶段 9 L2 总体审查。 |

## T7.3 专项细审

| Prompt 点名要求 | 正文证据位置 | 覆盖判断 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|
| 软缓存大小 | `## 资料与协议边界` 提到 TS 36.212 §5.1.4.1.2；`## 软缓存大小与协议参数` 列出 `Nsoft`、`NIR`、`Ncb`、`E`、`rvidx`；`## 协议证据表` 列出 source.docx 与 Word XML 证据。 | 覆盖。 | DL-SCH/PCH 主线 `N_IR` 与 `N_cb` 公式已在正文重建；UL-SCH/MCH 等 `N_cb = K_w` 边界已说明。 | 后续只需在系统级 HARQ 章节补 TS 36.213/36.321 调度侧锚点。 |
| 冗余版本 | `## 冗余版本的协议含义` 说明 `rvidx` 取值和 redundancy version number；`## 协议证据表` 记录 `rvidx` 证据；`## 数学定义与推导` 重建 `k_0` 起点公式。 | 覆盖。 | TS 36.213 HARQ/RV 调度来源仍作为后续接口边界，未在 T7.3 关闭。 | 另开系统级核验任务定位 TS 36.213 中 DL/UL HARQ RV 配置/时序章节，并在后续调度或 HARQ 章节补精确分册和章节。 |
| 增量冗余 | `## 冗余版本的协议含义` 说明不同窗口补充新位置；`## HARQ 软合并的直觉` 说明失败后保留 LLR 让重传补证据；RV 图展示四个窗口和重传模型。 | 覆盖。 | 与 Chase 合并的完整对比仍主要由 T4.3 承担，T7.3 已聚焦 LTE RV 地址语义。 | 后续全局互链时可补 T4.3 与 T7.3 的双向引用。 |
| 重传 LLR 更新缓存 | `## 数学定义与推导` 给出同一地址 LLR 合并公式；`## 接收端软缓存流程` 步骤 3 写明 `rx_llr[k]` 饱和累加到 `soft_buf[addr]`；`## 浮点仿真` 要求检查每次传输后覆盖/累加/中性地址；伪代码区分 `<NULL>` 跳过、首次写入和重复累加。 | 覆盖。 | 无阻塞缺口。 | 后续可在硬件章节继续扩展 soft buffer bank 和饱和加法器微架构。 |
| 循环缓存不同起点/区域 | `## 冗余版本的协议含义` 明确 `rvidx` 改变 circular buffer 起点；Mermaid 图和 `assets/T7.3_LTE_HARQ_RV_windows.png` 展示 RV0/RV1/RV2/RV3 不同窗口；`## 数学定义与推导` 重建协议 `k_0` 公式并解释其接收端地址意义。 | 覆盖。 | 无阻塞缺口。 | 后续若调整图片，需要继续执行无遮挡、间距和读图顺序审计。 |
| 小型循环缓存合并例子 | `## 数值例子：两次 RV 覆盖与软合并` 使用 `K_w=12`、`E=6`、`rvidx=0/2`，列出两次 LLR 和合并后 soft buffer；正文另有回绕与 `<NULL>` 跳过语义说明。 | 覆盖。 | 无阻塞缺口。 | 后续可在 T9 NR LDPC HARQ 中复用同类例子并改成 NR rate matching 语义。 |
| 定点饱和 | `## 数学定义与推导` 写“定点实现中的合并应使用饱和加法”；`## 定点化策略` 给出 6 bit 范围、`+28 + +10 -> +31`、`-30 + -8 -> -32`；`## 验证方法` 有定点合并测试，并与 T5.1/T2.4 的接口契约关联。 | 覆盖。 | 无阻塞缺口。 | 后续 RTL 章节可继续细化饱和加法树和缓存写回时序。 |
| 重传补充前一次被打孔或未发送的校验信息 | `## 冗余版本的协议含义` 写“补齐之前被打孔或未覆盖的校验信息”；`## 数值例子` 中地址 6-9 是新覆盖，10-11 仍未知；`## 与四类空洞的关系` 区分本次未发送和重复发送；正文说明例子是 circular buffer 地址级例子。 | 覆盖。 | 无阻塞缺口。 | 后续若需要三路流级别解释，应放到 T7.1/T7.2 的 rate matching 流程中，不混入 T7.3 的 HARQ 缓存主线。 |

## L3 模块 12 覆盖矩阵

| 讲义 | Roadmap Prompt 要求 | Roadmap 验收要求 | 正文中已有证据位置 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|:---|
| `docs/L3_工程实现/T17.1_python_golden_model_project_layout.md` | 规定 LTE Turbo、NR LDPC、NR Polar Python golden model 的包结构、配置文件、向量文件、随机种子、日志和可复现命令示例。 | Learner can scaffold a reproducible decoder simulation project. | `## 目录结构`、`## 配置文件 schema`、`## vector.json 最小 schema`、`## 随机种子派生规则`、`## metrics.csv 与 frames.jsonl`、`## 失败重跑`、`## 接收端工程流程`、`## 执行与证据记录`。 | 无阻塞缺口；只读审核提出的 seed 派生、vector schema、NR CRC 回链和 scaffold 验证口径已修复。真实 `sim/` 包未创建，正文已说明为规划模板。 | 后续 T13/T14/T15 使用本节 schema 时，应把真实 runner、vector 生成和 replay 命令落为可执行测试。 |
| `docs/L3_工程实现/T17.2_LTE_Turbo_float_sim_plan.md` | 定义 LTE Turbo 浮点仿真计划：编码器参考、AWGN 信道、解速率匹配、Log-MAP/Max-Log-MAP、CRC、BLER 曲线、随机种子、输出和阈值。 | Learner can run or implement a plan that produces LTE Turbo BLER curves and decoder traces. | `## 资料与协议边界`、`## 码率与 Eb/N0 口径`、`## 编码器参考`、`## 解速率匹配与 HARQ 软合并`、`## Log-MAP 与 Max-Log-MAP`、`## 随机种子与命令模板`、`## 阈值与验收`、`## 执行与证据记录`。 | 无阻塞缺口；审核指出的 T17.2 图形审计覆盖、AWGN 码率口径和 manifest/README 核验均已关闭。TS 36.213 MCS/TBS 仍按正文保留待核验边界。 | 后续真实仿真实现时生成 `metrics.csv`、`frames.jsonl`、失败 dump 和 BLER 曲线，并回填报告包路径。 |
| `docs/L3_工程实现/T17.3_NR_LDPC_float_sim_plan.md` | 定义 NR LDPC 浮点仿真计划：基图选择、提升、速率匹配/恢复、Min-Sum 变体、CRC、BLER 曲线、随机种子、输出和阈值。 | Learner can specify a reproducible NR LDPC BLER simulation and compare BP/MS/NMS/OMS. | `## 资料与协议边界`、`## 基图、提升与矩阵身份`、`## Rate Matching 与 Rate Recovery`、`## BP、SPA、MS、NMS 与 OMS 变体`、`## 变体比较命令矩阵`、`## 接收端伪代码`、`## 协议证据表`。 | 无阻塞缺口；审核提出的 BP/SPA 解释、命令矩阵、CBG/CBGTI/CBGFI 精确证据和关闭条件已修复；命令缩进已修正。 | 后续真实仿真实现时应把 BG/Zc/RV/CBG descriptor、syndrome trace 和算法变体曲线写入统一报告 schema。 |
| `docs/L3_工程实现/T17.4_NR_Polar_float_sim_plan.md` | 定义 NR Polar 浮点仿真计划：可靠性序列、速率恢复、SC/SCL/CA-SCL、列表大小扫描、CRC 检查、延迟代理指标、随机种子、输出和阈值。 | Learner can specify a reproducible CA-SCL performance experiment with list-size comparison. | `## 可靠性序列与信息集合`、`## Rate Recovery 与 LLR 初始化`、`## SC、SCL 与 CA-SCL`、`## 具体数值例子`、`## 列表大小扫描`、`## 随机种子与命令模板`、`## 阈值与验收`、`## 执行与证据记录`。 | 无阻塞缺口；审核经理 Popper 指出的阈值可执行例子、缩写首现和 SC/SCL/CA-SCL 二维命令矩阵已修复；横向复核确认 Prompt 覆盖充分。 | 后续真实仿真实现时应生成 list-size sweep、false-pass 统计、CRC/RNTI candidate dump 和延迟代理图。 |
| `docs/L3_工程实现/T17.5_BER_BLER_curve_reporting.md` | 讲解生成、保存、绘制和解读 BER/BLER 曲线，包含置信区间、最小帧数、仿真早停、CSV/PNG 命名和失败诊断。 | Learner can produce a report-ready BLER curve with reproducible seeds and metadata. | `## 统计推断基础`、`## 资料与协议边界`、`## 统计对象与分母`、`## 置信区间与零错误上界`、`## 最小帧数、最小错误数与早停`、`## 统一 metrics.csv Schema`、`## CSV、PNG 和报告命名规则`、`## 曲线生成命令模板`、`## 失败诊断流程`、`## 协议证据表`。 | 无阻塞缺口；审核指出的指标 CI/upper-bound 不完整、命令模板偏 `tb_bler`、UCI §6.3 锚点缺失已修复；本节未运行真实 BLER 仿真和 `decoder_golden.reporting` 命令，正文已明确这是报告规范与命令模板。 | 后续实现报告工具后，必须回填真实 CSV/PNG/PDF/report.json、config hash、样本数、NR Polar control BLER/false-pass 曲线和 replay 结果。 |

## L3 模块 13 覆盖矩阵

| 讲义 | Roadmap Prompt 要求 | Roadmap 验收要求 | 正文中已有证据位置 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|:---|
| `docs/L3_工程实现/T18.1_fixed_point_decoder_requirements.md` | 定义定点译码器需求：LLR 位宽、内部消息位宽、饱和规则、舍入、缩放、性能损失预算和 bit-exact 对比方案。 | Learner can write fixed-point requirements for one decoder block and identify comparison tolerances. | `## 固定点表示的最小理论`、`## 手算例子`、`## 统一 Fixed-Point Requirement 模板`、`## 三类译码器的位宽对象`、`## 性能损失预算`、`## Bit-Exact 对比方案`、`## RTL/ASIC 映射`、`## 执行与证据记录`。 | 无阻塞缺口；审核经理 Carson 指出的模板扩展不足和证据边界已修复，新增 `turbo.*`、`ldpc.*`、`polar.*`、`simd_memory.*`、`regression.*` namespace。真实 C/C++ 模型、边界整数、符号扩展、fixed-vs-float 曲线预算仍属于 T18.2-T18.6 的模型级验证闭环。 | 后续 T18.2-T18.6 必须按本节 namespace 填专用字段，并把真实 checkpoint manifest、failure bundle 和曲线预算回写到 requirement 版本。 |
| `docs/L3_工程实现/T18.2_LTE_Turbo_fixed_point_model_plan.md` | 规划 C/C++ 定点 LTE Turbo 译码器模型：分支度量、alpha/beta 度量、外信息缩放、交织器地址、饱和、max-log 修正选项和对 Python 的 bit-exact 测试。 | Learner can specify C/C++ structures and tests for a fixed-point Turbo decoder. | `## Turbo 定点模型的理论底座`、`## 输入输出与 C/C++ 对象`、`## 分支度量定点化`、`## Alpha/Beta 度量与归一化`、`## Log-MAP、Max-Log-MAP 与修正选项`、`## 外信息缩放与交织地址`、`## C/C++ 数组布局计划`、`## Bit-Exact 测试方案`、`## 协议证据表`、`## 执行与证据记录`。 | 无阻塞缺口；审核经理 Ohm 指出的 `branch_metric_sat_count`、`TurboFixedConfig` 字段、内部交织器公式/方向、$D=K+4$、rate matching 与接收端 soft buffer 边界、执行记录闭环和图形圆角均已修复。真实 `decoder_golden.fixed`、`lte_turbo_fixed_test`、fixed BLER 曲线未实现，正文已明确为计划和命令模板。 | 后续 T18.6 实现 bit-exact 回归时，应把 T18.2 的 `branch_metric`、`alpha_beta`、`extrinsic`、`crc` checkpoint 变成真实 trace schema 和 replay 命令。 |
| `docs/L3_工程实现/T18.3_NR_LDPC_fixed_point_model_plan.md` | 规划 C/C++ 定点 NR LDPC 译码器模型：LLR/消息位宽、最小/次小存储、归一化/偏移、layered 调度、饱和、syndrome 检查和 bit-exact 测试；给 NMS/OMS 实验口径与 BLER 损失比较方法。 | Learner can define fixed-point NMS/OMS experiments and compare BLER loss to floating point. | `## LDPC 定点模型的理论底座`、`## 输入输出与 C/C++ 对象`、`## LLR 与消息位宽`、`## CN Update：符号、Min1、Min2 与饱和`、`## NMS 与 OMS 定点参数`、`## Layered Read-Modify-Write`、`## C/C++ 数组布局计划`、`## Bit-Exact 测试方案`、`## NMS/OMS 实验与 BLER 损失比较`、`## RTL/ASIC 映射`、`## 执行与证据记录`。 | 无阻塞缺口；正文明确 TS 38.212 约束 BG/$Z_c$/rate matching，不强制定点 MS/NMS/OMS 位宽和 layered schedule。真实 `decoder_golden.fixed`、`nr_ldpc_fixed_test`、fixed BLER 曲线未实现，正文已标为计划和命令模板。图片表格字号问题已按全局新规修复，并新增 `tools/audit_figure_readability.py` 作为项目级风险发现工具。 | 后续 T18.6 应把 T18.3 的 `quantized_llr`、`cn_trace`、`layer_trace`、`posterior_snapshot`、`syndrome_crc` checkpoint 纳入统一 trace schema；全项目图片整改需继续处理 readability findings。 |
| `docs/L3_工程实现/T18.4_NR_Polar_fixed_point_model_plan.md` | 规划 C/C++ 定点 NR Polar 译码器模型：$f/g$ 函数、路径度量位宽、部分和、列表剪枝、排序器影响、CRC 辅助选择和 bit-exact 测试。 | Learner can specify fixed-point CA-SCL data structures and path metric saturation checks. | `## Polar 定点模型的理论底座`、`## 输入输出与 C/C++ 对象`、`## LLR、f 函数与 g 函数定点化`、`## Partial Sum 与 Path State`、`## 路径度量、饱和与排序剪枝`、`## CRC/RNTI 辅助选择`、`## Bit-Exact 测试方案`、`## RTL/ASIC 映射`、`## 执行与证据记录`。 | 无阻塞缺口；正文明确 TS 38.212 约束 Polar construction、CRC、rate matching、UCI/DCI/RNTI 边界，不强制定点 PM 公式、位宽、list size、sorter 或 path copy 策略。真实 C/C++ 定点模型、fixed control BLER/false-pass 曲线和真实 trace 未实现，正文已标为计划和命令模板。图片通过几何与可读性审计。 | 后续 T18.6 应把 T18.4 的 rate LLR、$f/g$ LLR、partial sum、candidate PM、sort/prune order、CRC/RNTI pass mask、selected path 和 path remap 纳入统一 trace schema。 |
| `docs/L3_工程实现/T18.5_SIMD_memory_layout_decoders.md` | 讲解 Turbo、LDPC、Polar C/C++ 译码器的内存布局、对齐、SIMD 友好数组、缓存局部性和向量化机会，包含 profiling 计划和非合并访存失败案例。 | Learner can propose an array layout for one decoder and justify cache/SIMD behavior. | `## SIMD 与缓存的最小理论`、`## AoS 与 SoA 的数据组织`、`## 对齐、padding 与 tail mask`、`## 三类译码器的 SIMD 友好布局总表`、`## LTE Turbo`、`## NR LDPC`、`## NR Polar`、`## Profiling 计划`、`## 非合并访存失败案例`、`## RTL/ASIC 映射`、`## 验证方法`、`## 执行与证据记录`。 | 无阻塞缺口；正文覆盖 prompt 全部硬要求，并扩展 packed/unpacked、hot/cold trace、false sharing、tail mask、layout hash、bit-exact baseline 和协议/实现边界。真实 SIMD C/C++ 二进制与 profiling 数据未实现，正文已明确为计划级模板。图片通过几何与可读性审计。 | 后续 T18.6 应把本节的 `layout_version`、`protocol_order_hash`、`simd_width_bits`、tail mask、A/B layout profiling 和 scalar-vs-SIMD compare 纳入统一 bit-exact 回归 schema。 |
| `docs/L3_工程实现/T18.6_bit_exact_regression_harness.md` | 设计 bit-exact 回归框架，对比 Python 浮点/定点参考、C/C++ 定点模型和后续 RTL 输出；包含向量格式、元数据、种子追踪、容差、通过/失败方案和 CI 命令。 | Learner can define a regression vector format and pass/fail policy for all three decoders. | `## bit-exact 的最小理论`、`## 回归流水线总图`、`## 统一 vector schema`、`## 种子追踪与确定性`、`## 容差、完全一致与通过/失败方案`、`## 三类译码器 checkpoint manifest`、`## 小型 HARQ soft combine 回归例子`、`## CI 命令模板`、`## 失败归档`、`## RTL/ASIC 映射`、`## 验证方法`、`## 执行与证据记录`。 | 无阻塞缺口；正文覆盖 prompt 全部硬要求，并扩展 metadata/hash 分层断言、float-vs-fixed 容差边界、fixed/C++/RTL exact integer policy、Turbo/LDPC/Polar checkpoint manifest、failure bundle、CI suite 分层和小型 HARQ soft combine 例子。真实 `decoder_golden.regression`、C/C++ 二进制、RTL scoreboard 和 CI 未实现，正文明确为框架/命令模板。图片通过几何与可读性审计，并按四项独立规则目检文字上下边距、边框间距、箭头和连线起点。 | 模块 14/15 应把本节 vector schema、checkpoint manifest、failure bundle 和 CI gate 落到 RTL scoreboard、覆盖率、DC/时序和最终 sign-off 报告。 |

## L3 模块 14 覆盖矩阵

| 讲义 | Roadmap Prompt 要求 | Roadmap 验收要求 | 正文中已有证据位置 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|:---|
| `docs/L3_工程实现/T19.1_LTE_Turbo_RTL_microarchitecture.md` | 设计 LTE Turbo RTL 微架构：SISO 数据通路、alpha/beta 存储、外信息存储、交织/解交织地址生成器、乒乓迭代控制、CRC 早停、时钟/复位方案和吞吐估算；给 block diagram、FSM 和存储规模估算。 | Learner can draw a Turbo decoder RTL block diagram and estimate key memories and control states. | `## 资料与协议边界`、`## 基础理论直觉`、`## RTL 微架构总览`、`## SISO 数据通路`、`## Alpha/Beta 存储`、`## 外信息 RAM 与交织地址生成`、`## 乒乓迭代控制与 CRC 早停`、`## 时钟、复位与异常处理`、`## 存储与吞吐估算`、`## 验证方法`、`## 执行与证据记录`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展基础理论直觉、数值例子、descriptor 字段、FSM 状态、错误处理、bit-exact checkpoint、RTL/ASIC 映射和图片四项独立审计记录。真实 RTL 代码、综合报告和真实 gate-level 时序尚未实现，正文已明确为架构规划。 | T19.4/T19.6 应复用本节 `TurboRtlDescriptor`、状态码、CRC early-stop 事件和 memory estimate 字段；T15 应把这些字段落到 verification plan、coverage 和 DC/时序报告模板。 |
| `docs/L3_工程实现/T19.2_NR_LDPC_RTL_microarchitecture.md` | 设计 NR LDPC RTL 微架构：layered 调度控制器、校验节点单元、变量节点更新、最小/次小数据通路、消息存储、LLR 存储、bank 冲突处理、syndrome/CRC 早停和吞吐估算。 | Learner can draw a layered LDPC decoder architecture and identify memory banking constraints. | `## 协议精读：§5.3.2 如何变成地址生成器`、`## 微架构总图`、`## RTL 模块划分`、`## CN Unit：符号、Min1、Min2 与目标边排除`、`## VN Read-Modify-Write 数据通路`、`## Memory Banking 专章`、`## Layered Decoder FSM 与早停边界`、`## 吞吐和存储估算`、`## 验证方法`、`## 执行与证据记录`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展 TS 38.212 §5.3.2/Table 5.3.2-1/2/3 到 QC 地址生成的协议精读、RMW 数值例子、CN min1/min2 手算例子、bank conflict 策略、descriptor/trace 字段、bit-exact checkpoint 和图片四项独立审计记录。真实 RTL 代码、综合报告和 gate-level 时序尚未实现，正文已明确为架构规划。 | T19.4/T19.6 应复用本节 `ldpc_task_ctrl` descriptor、`edge_schedule_hash`、`bank_conflict_count`、`pipeline_stall_count`、`syndrome_weight` 和 LDPC checkpoint 字段；T15 应把 bank conflict directed test、shift direction negative test 和 CN min1/min2 directed test 落到 RTL regression。 |
| `docs/L3_工程实现/T19.3_NR_Polar_RTL_microarchitecture.md` | 设计 NR Polar RTL 微架构：SC/SCL 树遍历、LLR 存储、部分和存储、路径存储、路径度量更新、排序/剪枝器、CRC 检查器和低延迟控制。 | Learner can draw a CA-SCL decoder architecture and explain sorter bottleneck. | `## 资料与协议边界`、`## 协议对象如何落到 RTL Descriptor`、`## Rate Recovery 到 Decoder Input`、`## SC/SCL 树遍历数据通路`、`## LLR Memory 设计`、`## Partial-Sum Memory`、`## Path Memory 与 Lazy Copy`、`## PM Update 与 Saturating Arithmetic`、`## Sorter/Pruner Bottleneck`、`## CRC/RNTI Final Selector`、`## Low-Latency FSM`、`## 数值例子`、`## RTL/ASIC 映射`、`## 验证方法`、`## 执行与证据记录`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展 TS 38.212 §5.3.1/§5.4.1 到 descriptor、rate-recovery input contract、path-state remap、lazy-copy 边界、`2L -> L` 手算例子、CRC/RNTI 选择器、low-latency FSM、bit-exact checkpoint 和图片四项独立审计记录。真实 RTL 代码、综合报告和 gate-level 时序尚未实现，正文已明确为架构规划。 | T19.4/T19.6 应复用本节 `PolarRtlDescriptor`、`polar.rate_recovery`、`polar.mask`、`polar.fg`、`polar.partial_sum`、`polar.pm_prune`、`polar.final` checkpoint 和 sorter/CRC status 字段；T15 应把 path remap、PM tie-break、CRC false-pass 和 timeout directed test 落到 RTL regression。 |
| `docs/L3_工程实现/T19.4_unified_decoder_subsystem_architecture.md` | 设计统一译码子系统，包含 Turbo、LDPC、Polar 引擎、共享输入/输出 DMA、软缓存、配置寄存器、中断/状态和错误处理；边界清晰，使每个引擎可独立测试。 | Learner can define top-level registers and dataflow for a multi-decoder accelerator. | `## 统一子系统的分层原则`、`## 基础理论直觉：统一的不是算法，而是所有权`、`## Common Descriptor 与 Family Extension`、`## Soft Buffer Key 与 RV Transaction`、`## DMA 数据流与所有权`、`## 具体数值例子`、`## 顶层寄存器分区`、`## 状态码、错误码和中断`、`## 顶层 FSM`、`## 每个引擎如何独立测试`、`## 验证方法`、`## 执行与证据记录`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展任务/数据/缓存/证据所有权理论、三任务数值走读、common descriptor/family extension 分区、soft-buffer 主 key 与 RV transaction 区分、DMA 所有权、状态码/错误码/IRQ、reset/timeout/abort、独立 engine mock 测试、集成回归和图片四项独立审计记录。真实 RTL、真实寄存器地址、真实 DMA/testbench/IRQ 仿真、综合和时序尚未实现，正文已明确为架构规划；TS 36.213/TS 38.331 等上游上下文保持 `待核验` 或 context evidence 边界。 | T19.5 应把 soft buffer key、RV transaction、atomic commit 和 release policy 展开为存储架构；T19.6 应把 common/family registers、status/error/IRQ 和字段来源落成寄存器配置流；T15 应把 mock DMA、family switch、soft-buffer miss、reset abort、IRQ sticky 和 bit-exact integration 落入 testbench/coverage。 |
| `docs/L3_工程实现/T19.5_soft_buffer_HARQ_memory_architecture.md` | 设计软缓存与 HARQ 存储架构：进程 ID、TB/CB/CBG 索引、RV 放置、饱和、存储 bank 划分、淘汰方案和 CRC 失败后的恢复；包含 LTE 与 NR 差异。 | Learner can propose a soft-buffer address map for LTE Turbo and NR LDPC. | `## 协议身份到存储对象的映射`、`## LTE Turbo 存储语义`、`## NR LDPC 存储语义`、`## 逻辑地址到 SRAM bank 的实现示例`、`## 饱和、掩码和事务日志`、`## 生命周期、淘汰和 CRC 失败恢复`、`## 小型 LTE 地址映射例子`、`## 小型 NR CBG 部分重传例子`、`## 接收端流程`、`## RTL/ASIC 映射`、`## 验证方法`、`## Prompt 覆盖验收表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展 soft buffer 作为证据库的理论直觉、ContextKey/BlockKey/AddrKey/Access 分层、LTE `NIR/Ncb/E/rvidx`、NR `rv/k0/E_r/CBGTI/CBGFI`、bank/row/lane 示例、事务日志、saturation counter、stale/observed/null mask、eviction policy、CRC fail recovery、最小 dump 包和图片四项独立审计记录。真实 `decoder_golden.softbuf`、真实 RTL、真实 SRAM macro、真实综合和时序尚未实现，正文已明确为架构规划；TS 36.213 精确锚点保持 `待核验`。 | T19.6 应把本节 `ContextKey/BlockKey/Access`、`harq_id/tb_epoch/cb_id/cbg_id/rvidx`、CBGTI/CBGFI、status/error 和 soft-buffer pressure 字段纳入寄存器配置流；T15 应把 LTE RV overlap、NR CBGTI hold、CBGFI flush、reset during transaction 和 bank conflict 落入 RTL regression。 |
| `docs/L3_工程实现/T19.6_decoder_register_map_configuration_flow.md` | 定义译码器寄存器表和配置流：算法选择、块长、码率、BG、Zc、RV、Qm、HARQ ID、列表大小、迭代上限、start/status/error 中断，并追踪字段来自 PHY/MAC/RRC 的哪里。 | Learner can map high-level protocol parameters to hardware registers. | `## 资料与协议边界`、`## 寄存器不是协议字段`、`## 字段分类`、`## 寄存器表总览图`、`## 寄存器分组与字段定义`、`## 字段来源映射`、`## 配置状态机`、`## 具体数值走读：三个配置例子`、`## 软件驱动伪代码`、`## RTL/ASIC 映射`、`## 验证方法`、`## Prompt 覆盖验收表`、`## 协议证据表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展寄存器不是 3GPP 空口字段的分层解释、协议/调度/MAC/RRC/实现策略五类字段来源、CAPABILITY/COMMON/SOFTBUF/TURBO/LDPC/POLAR/CONTROL/STATUS/ERROR_IRQ/TRACE 十类寄存器、shadow lock 配置流、descriptor hash、三类任务数值走读、driver 伪代码、错误码、IRQ、验证计划和图片四项独立审计记录。真实 RTL、真实 APB/AXI-Lite 地址、真实 MAC/RRC 字段解析器、TS 36.213 精确字段、TS 36.321/36.331/38.321/38.331 精确字段仍未逐条核验，正文已标边界。 | T15 应把本节寄存器字段、状态码、错误码、IRQ sticky、busy 写保护、descriptor hash 和待核验字段闭环纳入 testbench、directed tests、coverage bins、DC/时序报告和最终证据报告。 |

## L3 模块 15 覆盖矩阵

| 讲义 | Roadmap Prompt 要求 | Roadmap 验收要求 | 正文中已有证据位置 | 明显缺口 | 建议补写动作 |
|:---|:---|:---|:---|:---|:---|
| `docs/L3_工程实现/T20.1_decoder_testbench_architecture.md` | 设计 Turbo、LDPC、Polar 译码引擎的 SystemVerilog testbench 架构，包含 driver、monitor、scoreboard、参考向量加载器、断言、复位测试和超时方案。 | Learner can write a testbench plan that compares RTL output to golden vectors. | `## Testbench 总体架构`、`## Reference Vector Loader`、`## Driver 设计`、`## Monitor 设计`、`## Scoreboard 设计`、`## Assertions`、`## Reset 与 Timeout 测试`、`## 具体数值例子：一条 NR LDPC CBG 重传向量`、`## 三类译码器 directed tests`、`## Output Artifacts 与 Failure Bundle`、`## 验证方法与验收口径`、`## 协议证据表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展 testbench 工程动机、T18.6 vector schema 到 RTL 的映射、T19.6 register driver 顺序、monitor 四类事件、scoreboard 六层比较、SVA 不变量、DUT/TB 双 timeout、NR LDPC CBG 重传走读、三类 decoder directed tests、failure bundle、UVM/非 UVM 映射和图片四项独立审计记录。真实 SystemVerilog RTL 仿真入口、UVM 环境、simulator 命令和真实波形尚未实现，正文已明确为架构规划和命令边界。 | T20.2 应生成 exact protocol vector/corner-case suite，供本节 loader 和 directed tests 消费；T20.3 应把本节 directed tests、assertions、timeout 和 failure classes 转为 coverage bins 与 regression tiers；T20.6 应把 failure bundle、scoreboard summary 和 known limitations 纳入最终证据报告。 |
| `docs/L3_工程实现/T20.2_protocol_vector_corner_case_suite.md` | 定义 LTE Turbo、NR LDPC、NR Polar 的协议向量和边界案例套件，覆盖最小/最大大小、填充位、CRC 失败、RV 不匹配、CBG、列表大小压力、LLR 饱和和运行中复位。 | Learner can list required directed tests for all three decoders and explain what each catches. | `## 协议向量不是随机样本`、`## 向量文件对象模型`、`## Suite Tier 与通过条件`、`## LTE Turbo Directed Vector Suite`、`## NR LDPC Directed Vector Suite`、`## NR Polar Directed Vector Suite`、`## Cross-Family Vectors`、`## 具体数值例子：小型循环缓存合并向量`、`## RTL/ASIC 映射`、`## Prompt 覆盖验收表`、`## 协议证据表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展协议向量对象模型、`suite_manifest.json`、`suite_policy.json`、positive/negative expected-fail 口径、三类 decoder directed case 表、最小 dump 包、cross-family reset/timeout/LLR/sign/hash vectors、循环缓存合并数值例子、Python 分类片段、图片四项独立审计记录和引用重建候选解释。真实 `decoder_vector_gen`、`decoder_regress`、RTL 仿真和真实向量文件尚未实现，正文已明确为格式和策略规划。 | T20.3 应把本节 suite tier、case id、expected fail class、reset/timeout、LLR saturation、family cases 转成 coverage bins、nightly tiers 和 sign-off 标准；T20.6 应汇总本节 vector schema、failure bundle 和协议证据。 |
| `docs/L3_工程实现/T20.3_coverage_regression_strategy.md` | 定义译码验证的功能覆盖率、代码覆盖率、回归层级、随机种子、夜间运行、失败分诊和 sign-off 标准；覆盖算法家族、块长、RV、Qm、CRC 状态和复位。 | Learner can define coverage bins and regression pass criteria for a decoder subsystem. | `## 覆盖率的最小理论`、`## 功能覆盖率对象模型`、`## 三类译码器的覆盖矩阵`、`## 代码覆盖率与断言覆盖率`、`## 回归层级`、`## 随机种子策略`、`## Nightly 运行计划`、`## 失败分诊`、`## Sign-off 标准`、`## Prompt 覆盖验收表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展 coverage event 对象模型、LTE/LDPC/Polar family-specific bins、LDPC `BG x Zc x RV x CBG` cross、Polar `context x crc_len x list_size` cross、deterministic seed 公式、nightly 五阶段计划、failure class 表、sign-off gate 和小型 Python aggregator。真实 RTL coverage DB、UCDB/FSDB/VCD、`decoder_regress` 和 CI runner 尚未实现，正文已明确为计划和命令模板。 | T20.4/T20.5 应把 coverage/sign-off 输出接入综合/时序风险记录；T20.6 应把 coverage DB、nightly summary、failure index、waiver list 和 sign-off checklist 纳入最终证据报告。 |
| `docs/L3_工程实现/T20.4_DC_synthesis_flow_decoders.md` | 讲解译码 RTL 的 Synopsys Design Compiler 综合流程：文件列表、时钟约束、复位假设、compile 方案、时序报告、面积报告、功耗估算和常见关键路径；若未安装 DC，必须说明工具可用性限制。 | Learner can prepare a DC script skeleton and interpret timing/area reports. | `## 工具可用性边界`、`## 文件列表`、`## DC Tcl 脚本骨架`、`## 时钟约束`、`## 复位假设`、`## 输入输出延迟与路径例外`、`## Compile 方案`、`## 时序报告解读`、`## 面积报告解读`、`## 功耗估算`、`## 常见关键路径`、`## 具体数值例子：用小型 timing record 分类`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并明确当前环境 `dc_shell`/`design_vision` 不可用，不能声称真实综合已跑过。正文扩展了 filelist 审查表、DC Tcl 骨架、SDC 示例、reset/false path 风险、compile option 取舍、timing/area/power 报告字段、译码器关键路径族、post-synthesis 协议向量回链和小型 timing triage Python 片段。真实 DC、工艺库、mapped netlist、timing/area/power 报告和 gate-level replay 尚未实现。 | T20.5 应把本节列出的 Turbo ACS、LDPC min tree、Polar sorter、soft buffer merge 和 unified top 路径展开成时序收敛诊断；T20.6 应把 DC 工具边界、报告路径、未运行项和后续证据模板纳入最终报告。 |
| `docs/L3_工程实现/T20.5_timing_closure_decoder_critical_paths.md` | 讲解译码 RTL 时序收敛：关键路径识别、LDPC check-node min tree、Polar sorter、Turbo ACS/度量更新、流水线、retiming、寄存器复制和面积/时序取舍。 | Learner can diagnose one plausible timing violation and propose a pipeline or architecture fix. | `## 关键路径识别`、`## LDPC Check-Node Min Tree`、`## Polar Sorter`、`## Turbo ACS 与度量更新`、`## Pipeline、Retiming 与寄存器复制`、`## 面积、时序与功耗取舍`、`## 具体数值例子：Polar Sorter 违例`、`## 修复后的验证方法`、`## Prompt 覆盖验收表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展 timing report 字段、path family 分类、LDPC min1/min2 比较树、Polar sorter PM/compare/lazy-copy 路径、Turbo ACS/normalization 对齐风险、pipeline/retiming/register duplication/tree split/banking 选择流程、面积/时序/功耗/延迟/验证取舍、Polar sorter 负 slack 数值例子、Python 分诊片段、自测答案和图片四项独立审计记录。当前仓库没有真实 STA/timing report，正文明确本节为教学违例样例和流程模板，不能声称真实时序已收敛。 | T20.6 应汇总本节 timing violation 分类、修复策略、回归向量、coverage bin、STA 工具边界、未运行真实报告项和 sign-off 证据模板。 |
| `docs/L3_工程实现/T20.6_final_decoder_verification_evidence_report.md` | 定义最终验证与证据报告格式：协议证据表、仿真摘要、定点损失、RTL 回归、覆盖率、综合时序/面积/功耗、已知限制和 sign-off 清单。 | Learner can assemble an audit-ready final report for LTE Turbo, NR LDPC, and NR Polar decoder work. | `## 最终报告的对象模型`、`## 协议证据汇总表`、`## 仿真摘要格式`、`## 定点损失摘要格式`、`## RTL 回归摘要格式`、`## 覆盖率摘要格式`、`## 综合、时序、面积和功耗摘要格式`、`## 已知限制与 Waiver 规则`、`## Sign-off 清单`、`## 具体数值例子：一条 NR LDPC CBG 重传证据包`、`## 最终报告 JSON Schema 草案`、`## Python 证据清单校验片段`、`## Prompt 覆盖验收表`、`## 协议证据表`。 | 无阻塞缺口；正文覆盖 Prompt 全部硬要求，并扩展最终证据对象模型、具体 Rel-19 协议锚点、真实工程证据边界、manifest schema、Python 结构校验、sign-off gate、known limitations/waiver 规则和图片四项独立审计记录。当前仓库没有真实完整 BLER campaign、真实定点损失结果、真实 SystemVerilog RTL regression、真实 coverage database、真实 DC mapped netlist 或真实 timing/area/power reports；正文明确最终签核状态应保持 `hold`，不能把模板写成真实通过。 | 模块 15 收尾应运行 T20.1-T20.6 合并审计和全项目图片几何/可读性审计；后续真实工程阶段按本节 schema 归档仿真、定点、RTL、coverage 和综合证据。 |

## 未纳入矩阵的 roadmap 卡片

当前 roadmap 卡片在文件系统中均有对应 Markdown 讲义；本节无未纳入矩阵的 T 类讲义。后续新增讲义或重命名文件时，需要重新运行覆盖审计。

## 建议优先级

1. 优先整改 5 篇 L1 深度 triage 文档，压缩重复边界/待核验表述，同时保留协议边界和关闭条件。
2. 其次关闭 T7.2 Table 5.1.4-1 与 T6.4 Table 5.1.3-3 的“输入数据、生成脚本、输出资产”证据链，保证复杂协议表复现可审计。
3. 对引用重建候选做完整分类：协议表/图/公式必须复现项、基础课背景项、已由其他章节承接项、仍待核验项。
4. 对基础课不要强行补协议细节；只需把“背景锚点、待核验状态、由后续哪节关闭”写清，避免把实现策略误写成 3GPP 强制规则。
