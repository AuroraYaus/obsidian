# Codex 3GPP协议｜Obsidian图谱标准化

## 项目背景

我本地Obsidian已有大量整理完毕的3GPP 5G信道译码笔记，据此完成Obsidian图谱标准化。

图谱核心规则：**仅使用Obsidian内置全局Graph视图作为核心图谱载体**；Mermaid/PlantUML仅作为单笔记局部辅助示意图。

绘图智能选型规则：

- 实体总数 ≤20：使用 Mermaid

- 实体总数 ＞20：自动切换 PlantUML

- 禁止用Mermaid承载大型全量协议关系，避免卡顿、错乱

节点分类、Obsidian图谱配色与样式全部由AI自行决定，无需向用户确认。

---

## 一、实体提取规则

整合存量笔记与新增输入协议文本，提取四类独立节点，**所有协议明确定义的术语、参数、信道、算法、标准，均单独创建独立节点，不合并、不遗漏**。

固定四类实体Type：

- **type:spec**：3GPP协议规范、协议归纳文档

- **type:channel**：物理上下行信道（PUSCH、PDSCH等）

- **type:algorithm**：编译码功能模块（LDPC、码块分割、速率匹配、交织等）

- **type:definition**：协议术语、传输参数定义（TB、CB、CG、RV、码率等）

规范约束：

1. 已有旧笔记仅输出缺失的frontmatter片段（type、aliases、tags、source\_spec），不重写全文，适配在线粘贴修改头部元数据；

2. 所有definition节点必须附带**协议原文定义**，通过source\_spec标注对应协议编号；

3. 同义术语统一配置在aliases字段，**禁用管道别名语法\[\[xxx\|别名\]\]**。

---

## 二、零空心节点强制规则（核心红线）

不从图谱视图隐藏空节点，从链接生成逻辑根源杜绝Obsidian灰色前沿空心节点产生。

1. 仅对库内**真实存在的md文件**，允许添加\[\[wikilink\]\]双向链接；

2. 新识别、未创建的协议概念、术语、参数，**完全不写任何\[\[wikilink\]\]**，仅用\#标签临时标记；

3. 输出末尾统一生成《待新建节点清单》，必须完成清单内全部文件新建工作后，才可批量补充双向链接；

4. 未落地的节点不参与任何图谱链接关系，彻底杜绝空节点源头。

---

## 三、Obsidian图谱上色配置输出要求

AI自主完成四类节点配色设计，适配Obsidian深色主题，配色区分度高、视觉无冲突，输出两套可直接落地的配置方案：

1. **图谱颜色分组配置**：提供基于type元数据的完整筛选查询语句 \+ 对应定制色值，可直接复制导入Obsidian图谱；

2. **Graph专用CSS片段**：全局永久固定四类节点颜色，同时弱化违规悬空节点，做兜底防护；

必须附带**配色图例表格**，清晰展示：type类别\-对应色值\-业务含义。

---

## 四、关联关系规范

所有关联仅在已存在的真实文件之间建立双向链接，杜绝无效关联，关系语义清晰可追溯：

1. spec 协议文档 ↔ 所属 channel信道 / algorithm算法 / definition定义

2. algorithm 编译码算法 ↔ 自身依赖的 definition参数定义

每条关联必须标注明确语义：包含、规定、依赖、应用、定义。

---

## 五、固定输出顺序（严格遵守，不可打乱）

1\. 存量笔记元数据补全片段（仅输出frontmatter内容，适配在线修改）
2\. 新增概念md完整模板（标准化结构，可直接新建文件）
3\. 待新建节点清单（汇总所有未落地新概念）
4\. Obsidian图谱全套上色配置（图例表格\+分组筛选语句\+完整CSS片段）
5\. 简易分步落地操作流程（适配3GPP项目落地）
6\. 局部辅助示意图（智能判断实体规模，自动选用Mermaid/PlantUML，仅作补充，不替代原生图谱）

---

## 六、额外全局约束

- 充分复用现有3GPP归纳内容，禁止重复生成已有完整笔记，仅做查漏补缺、标准化规整；

- 所有输出格式干净整洁、无冗余废话，适配Obsidian直接粘贴编辑；

- 全程以Obsidian原生Graph图谱为唯一核心，辅助绘图仅做局部参考。

> （注：部分内容可能由 AI 生成）
